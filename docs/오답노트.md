# 오답노트 — 일일 시장 리포트 반복 오류 정리

> 오류 발생 시 여기에 추가. 원인·해결책·예방법을 기록해 같은 실수를 반복하지 않는다.

---

## #001 VKOSPI 수집 실패 (409 StockConflict)

| 항목 | 내용 |
|------|------|
| **현상** | `domestic.vkospi` 가 null, 리포트에 "미국 VIX (참고)" 표시 |
| **원인** | Naver 모바일 API (`m.stock.naver.com/api/index/VKOSPI/basic`) 가 상장지수에 VKOSPI를 포함하지 않아 409 반환. 08:00 KST 장전 시간에는 영구적으로 실패 |
| **해결** | 4단계 폴백: ① Naver sise_index HTML 파싱 → ② Naver mobile API (2회 재시도) → ③ 전일 `data.json` carry-forward → ④ Yahoo ^VIX |
| **예방** | 16:40 KST `supply_snapshot.js` 에서 VKOSPI를 수집해 `supply.json`에 저장 → 익일 STEP 2.5에서 병합 |
| **관련 파일** | `tools/collectors/domestic.js`, `tools/collectors/supply_snapshot.js`, `tools/main.js` (STEP 2.5) |
| **발생일** | 2026-05-09 ~ 2026-05-12 |

---

## #002 KOSPI 수급 데이터 항상 null

| 항목 | 내용 |
|------|------|
| **현상** | 외국인/기관/개인 수급 바가 리포트에 표시되지 않음 |
| **원인** | Naver `polling.finance.naver.com/api/realtime/domestic/index/KOSPI_INVESTOR` 가 장전(08:00 KST)과 장중에는 `datas: []` 반환. 오후 장 마감(16:30) 후에만 정상 데이터 제공 |
| **해결** | 별도 GitHub Actions 잡 (`supply-collect.yml`) 을 16:40 KST에 실행. `outputs/{날짜}/supply.json` 저장 → 익일 08:00 리포트에서 병합 |
| **예방** | 리포트 수집 시점(08:00 KST)에서는 수급을 직접 수집하려 하지 말 것. 항상 전일 `supply.json`에 의존 |
| **관련 파일** | `.github/workflows/supply-collect.yml`, `tools/collectors/supply_snapshot.js`, `tools/main.js` (STEP 2.5) |
| **발생일** | 2026-05-09 ~ 2026-05-12 |

---

## #003 GitHub Actions 크론 대규모 지연

| 항목 | 내용 |
|------|------|
| **현상** | 08:00 KST 발송 예정 리포트가 실제로 17:00 KST 전후에 도착 (약 9시간 지연) |
| **원인** | GitHub Actions 크론은 UTC 기준이며, 서버 부하에 따라 수십 분~수 시간 지연 가능. 무료 플랜에서 특히 두드러짐 |
| **현황 대응** | 크론 미실행 시 로컬에서 `node tools/main.js --now --dry-run && node tools/preview_send.js` 수동 실행 후 발송 |
| **개선 방향** | 크론 시간을 더 여유 있게 설정하거나 (예: 07:00 KST = 22:00 UTC), 별도 외부 스케줄러(cron-job.org 등)에서 `workflow_dispatch` API 호출 고려 |
| **관련 파일** | `.github/workflows/daily-report.yml` |
| **발생일** | 지속적 (2026-05 기준) |

---

## #004 한경컨센서스 API 500 오류

| 항목 | 내용 |
|------|------|
| **현상** | `[tf-analyst] 한경 컨센서스 수집 실패: Request failed with status code 500` |
| **원인** | `consensus.hankyung.com/apps.analysis/analysis.list` 엔드포인트가 간헐적으로 500 반환. 서버 측 문제로 보임 |
| **해결** | `try/catch`로 실패 시 빈 배열 반환, 뉴스 기반 분석만으로 계속 진행. 리포트 자체는 중단되지 않음 |
| **예방** | 외부 스크래핑은 항상 실패를 전제로 설계. 한경컨센서스 없이도 뉴스 기반으로 3건 선정 가능하도록 프롬프트 구성 유지 |
| **관련 파일** | `tools/teams/tf_analyst.js` (`fetchHankyungConsensus`) |
| **발생일** | 2026-05-12 ~ (간헐적) |

---

## #005 HTML 리포트 서식이 레퍼런스와 불일치

| 항목 | 내용 |
|------|------|
| **현상** | 발송된 메일의 HTML이 `templates/market_report_reference` 디자인과 다름. 헤더 레이아웃, 수급 섹션, 애널리스트 섹션 누락 |
| **원인** | `preview_send.js`가 자체 HTML 생성 로직(712줄)을 갖고 있었고, `designer.js`의 `buildHtml()`을 사용하지 않았음. `include_analyst: false` 하드코딩 |
| **해결** | `preview_send.js`를 `buildHtml()` 호출 방식으로 리팩터. `include_analyst`를 `tfAnalystResult.findings?.length > 0`으로 동적 처리 |
| **예방** | HTML 생성 로직은 반드시 `designer.js` 단일 진입점으로만. `preview_send.js`는 오케스트레이션만 담당 |
| **관련 파일** | `tools/preview_send.js`, `tools/desk/designer.js` |
| **발생일** | 2026-05-12 (발견 및 수정) |

---

## #006 뉴스 정렬 순서 미적용

| 항목 | 내용 |
|------|------|
| **현상** | 리포트에서 뉴스가 시장전반 → 거시경제 → 산업·기업 순서로 정렬되지 않음 |
| **원인** | `news.js`에서 수집 시 정렬을 적용했지만, 기존 `data.json`은 이미 저장되어 있어 재정렬 불가. `designer.js`에서 렌더링 시 재정렬하지 않음 |
| **해결** | `designer.js`의 `_assembleHtml`에서 `NEWS_CAT_ORDER` 기준으로 항상 정렬 후 렌더링 |
| **교훈** | 데이터 저장(수집 시) 정렬과 렌더링 시 정렬 모두 적용할 것. 데이터가 이미 저장된 경우를 대비해 렌더링 레이어에서도 방어 |
| **관련 파일** | `tools/collectors/news.js`, `tools/desk/designer.js` |
| **발생일** | 2026-05-12 |

---

## #007 sent.flag 날짜 기준 오류 (중복 발송 / 발송 차단)

| 항목 | 내용 |
|------|------|
| **현상** | 이미 발송된 리포트가 재발송되거나, 정상 실행인데 "이미 발송됨"으로 차단 |
| **원인** | `sent.flag` 경로가 `outputs/{오늘날짜}/sent.flag` 기준인데, GA 서버의 TZ 설정이나 날짜 계산 오류로 경로가 다른 날짜를 참조 |
| **예방** | `TZ: Asia/Seoul` 환경 변수를 반드시 워크플로우에 명시. `sent.flag` 생성 전 경로 로그 확인 |
| **관련 파일** | `tools/preview_send.js`, `.github/workflows/daily-report.yml` |
| **발생일** | 간헐적 |

---

## #008 GitHub Actions 크론 지연 → supply_snapshot 날짜 오류

| 항목 | 내용 |
|------|------|
| **현상** | VKOSPI·수급이 리포트에 여전히 빈값으로 표시됨. supply_snapshot이 성공했음에도 다음날 리포트에서 데이터를 읽지 못함 |
| **원인** | 크론이 16:40 KST 대신 01:32 KST(다음날 새벽)에 실행되면, `todayStr`이 다음날 날짜가 됨. 예: supply_snapshot이 `outputs/2026-05-13/supply.json`에 저장했지만, 리포트는 `outputs/2026-05-12/supply.json`을 읽음 → 파일 없음 |
| **해결** | `supply_snapshot.js`에서 KST 시각 < 09:00이면 전 영업일 날짜로 후퇴 후 저장. 근본 원인은 `cron-job.org` 외부 스케줄러로 workflow_dispatch 호출해 정확한 시각 보장 |
| **예방** | supply_snapshot은 "오늘 날짜"가 아닌 "최근 장 마감 영업일" 기준으로 저장해야 함. 크론 실행 시각에 의존하지 말 것 |
| **관련 파일** | `tools/collectors/supply_snapshot.js` (kstHour < 9 분기 추가), `.github/workflows/supply-collect.yml` |
| **발생일** | 2026-05-13 |

---

## #009 AI Summary(요약) 항상 비어있음

| 항목 | 내용 |
|------|------|
| **현상** | 리포트 상단 AI Summary 박스가 비어있거나 렌더링되지 않음 |
| **원인** | `preview_send.js`가 `tf_analyst.js`만 호출하고 `tf_news.js`를 호출하지 않음. `buildHtml()`의 `tfResults.news.top_stories`가 항상 `undefined` → `_buildSummarySection()`이 빈 문자열 반환 |
| **해결** | `preview_send.js`에 `runTFNews(news, data)` 호출 추가. `buildHtml` 두 번째 인수에 `{ news: tfNewsResult, analyst: tfAnalystResult }` 전달 |
| **예방** | `buildHtml()` 호출 시 `tfResults.news`가 채워져 있는지 항상 확인. `preview_send.js` 수정 시 두 TF팀 모두 호출하는지 체크 |
| **관련 파일** | `tools/preview_send.js`, `tools/desk/designer.js` (`_buildSummarySection`) |
| **발생일** | 2026-05-13 (발견 및 수정) |

---

## #010 헤더 데이터 출처 표기 누락

| 항목 | 내용 |
|------|------|
| **현상** | 리포트 헤더 날짜 옆에 "— 한국경제 · 네이버증권" 출처 표기가 없음 |
| **원인** | `designer.js` 헤더 템플릿에서 `dateFull` 뒤에 출처 텍스트를 포함하지 않았음 |
| **해결** | `${dateFull} 종가 기준 — 한국경제 · 네이버증권` 으로 수정 |
| **예방** | 레퍼런스 템플릿(`templates/market_report_reference`)의 헤더 구조를 그대로 유지할 것. 수정 전 반드시 레퍼런스와 비교 |
| **관련 파일** | `tools/desk/designer.js` (`_assembleHtml` 헤더 부분) |
| **발생일** | 2026-05-13 (발견 및 수정) |

---

## #011 수급 데이터 소급 수집 불가 (Naver polling API 한계)

| 항목 | 내용 |
|------|------|
| **현상** | 특정 날짜(예: 5/12) 수급 데이터를 장 마감 후 다음날 소급 수집 시도 → 전부 실패 |
| **원인** | `polling.finance.naver.com/api/realtime/domestic/index/KOSPI_INVESTOR`는 **당일 실시간 API**. 어제 날짜 조회 파라미터가 없고, 다음날 장이 열리면 이전 데이터 접근 불가. Naver 금융 HTML 페이지는 수급 숫자를 JavaScript로 렌더링(서버 HTML에 없음). KRX 데이터포털은 브라우저 세션/CSRF 쿠키 필수 |
| **시도했던 방법** | ① Naver polling API → `datas:[]` ② Naver `investorDealTrendDay.naver` HTML 파싱 → 빈 결과 ③ KRX `getJsonData.cmd` POST → 400/403 ④ KRX OTP 방식 → 403 |
| **결론** | 수급 데이터는 **당일 16:30~16:40 사이에만** 수집 가능. 그 이후는 소급 불가 |
| **예방** | `supply-collect.yml`을 cron-job.org 외부 스케줄러로 정확히 16:40 KST에 실행. GA 크론만 믿으면 지연/누락 발생 → supply.json이 생성되지 않아 수급 카드 영구 미표시 |
| **현재 설정** | cron-job.org jobId: `7594700` — 16:40 KST 평일 자동 실행 |
| **관련 파일** | `tools/collectors/domestic.js` (`fetchKrxSupply`), `tools/collectors/supply_snapshot.js`, `.github/workflows/supply-collect.yml` |
| **발생일** | 2026-05-13 |

---

## #012 수급 카드 미표시 — supply-collect.yml cron-job.org 누락

| 항목 | 내용 |
|------|------|
| **현상** | 리포트에 외국인/기관/개인 수급 카드 대신 "시장 강도" 카드만 표시 |
| **원인** | `daily-report.yml`에만 cron-job.org 외부 스케줄러를 연결하고 `supply-collect.yml`은 GA 크론만 사용 → GA 크론 지연/미실행으로 `supply.json`이 한 번도 생성되지 않음 → `d.supply.foreign/institution/individual`이 모두 `null` → `hasSupply = false` → 수급 카드 생략 → breadth 폴백 |
| **해결** | cron-job.org에 `supply-collect.yml`용 잡 추가 (jobId: `7594700`, 16:40 KST 평일) |
| **예방** | 새 GA 워크플로우 추가 시 **반드시** cron-job.org 잡도 함께 등록. GA 크론만으로는 시간 보장 불가 |
| **관련 파일** | `.github/workflows/supply-collect.yml`, cron-job.org jobId: `7594700` |
| **발생일** | 2026-05-13 (발견 및 수정) |

---

## 빠른 디버깅 체크리스트

리포트가 안 오면 이 순서로 확인:

```
1. GitHub Actions 실행 여부 확인
   → https://github.com/neofil0308-commits/daily-market-report/actions

2. 오늘 sent.flag 존재 여부 확인
   → outputs/YYYY-MM-DD/sent.flag

3. 로컬 수동 실행으로 직접 발송
   → node tools/main.js --now --dry-run
   → node tools/preview_send.js

4. 오류 로그 확인
   → GA Actions 탭 → 실패한 step 클릭
   → 로컬: 콘솔 출력 확인
```

---

## #013 내용 미확정 상태에서 구현 선진행

| 항목 | 내용 |
|------|------|
| **현상** | 이메일 카드에 넣을 섹션(증시·환율·코인·뉴스 구성)을 사주와 합의하기 전에 임의로 구현하여 방향 재설정이 필요해짐 |
| **원인** | "한번 구현해볼래?"라는 말을 내용 확정 신호로 오해. 사주가 직접 "카드뉴스의 내용을 확정하지 않았는데 슉슉 진행해버렸네"라고 지적 |
| **해결** | 구현 범위가 넓은 작업은 반드시 **항목 목록·데이터 구성·화면 배치를 먼저 문서화 후 승인** 받고 진행 |
| **예방** | 디자인·콘텐츠 작업 시작 전 체크리스트: ① 어떤 데이터를 표시할지 ② 섹션 순서 ③ 레이아웃 방식 — 세 항목 모두 사주 확인 후 코딩 시작 |

---

## #014 깨진 구조 위에 반복 패치 (Incremental patching on broken structure)

| 항목 | 내용 |
|------|------|
| **현상** | 이메일 카드 UX 개선을 10회 이상 반복했음에도 매번 "전혀 반영이 안 된 것 같다", "로그처럼 보인다" 피드백이 반복됨 |
| **원인** | 구조 자체가 잘못됐는데(테이블 형식 vs 카드 그리드 혼용, flex 컬럼 너비 미설정 등) 수치(패딩, 폰트크기)만 조정하는 방식으로 반복 수정. 근본 문제를 건드리지 않으면 아무리 많이 수정해도 결과가 같음 |
| **해결** | 2~3회 피드백 이후에도 같은 문제가 반복되면 → **즉시 전체 재설계 결정**, 기존 코드에 덧대지 않음 |
| **예방** | 피드백을 받았을 때 자문: "이 문제가 수치 조정으로 해결되는가, 아니면 구조를 바꿔야 하는가?" 구조 문제면 패치 금지 |

---

## #015 코드 수정 후 미리보기 미갱신 → preview와 실제 이메일 불일치

| 항목 | 내용 |
|------|------|
| **현상** | `outputs/email_preview.html`이 최신 `buildEmailCard`와 다른 내용을 표시. 사주가 "preview 내용과 실제 이메일이 다르다"고 지적 |
| **원인** | 디자인 에이전트가 리뉴얼 직후 preview를 한 번 생성하고, 이후 코드를 3번 더 수정하는 동안 preview를 갱신하지 않음 |
| **해결** | `buildEmailCard` 수정 후 항상 아래 명령으로 preview 재생성: `node --input-type=module -e "import {buildEmailCard} from './tools/desk/designer.js'; ..."` |
| **예방** | `buildEmailCard` 수정 → preview 재생성 → 브라우저 확인 → 이메일 발송 순서를 **반드시 지킬 것**. 이메일 발송이 "테스트"가 되면 안 됨 |
| **관련 파일** | `tools/desk/designer.js`, `outputs/email_preview.html` |

---

## #016 디자인 에이전트 과다 위임으로 컨텍스트 단절

| 항목 | 내용 |
|------|------|
| **현상** | 같은 디자인 작업을 에이전트에게 여러 번 위임했지만 매번 이전 맥락을 모르는 상태에서 시작, 같은 실수 반복 (테이블 형식 → 카드 → 테이블 반복 등) |
| **원인** | 에이전트는 새 호출마다 컨텍스트가 초기화됨. 지시가 불충분하거나 제약 조건이 빠지면 이전에 해결한 문제가 다시 나타남 |
| **해결** | 에이전트 위임 시 프롬프트에 반드시 포함: ① 현재 코드의 정확한 위치(줄 번호) ② 이미 시도했고 실패한 방법 ③ 절대 바꾸면 안 되는 제약 ④ 목표 HTML 구조 명세(의사코드 수준) |
| **예방** | 핵심 제약(인라인 스타일만, table 태그 금지, CSS 변수 금지 등)은 에이전트 프롬프트 **맨 마지막**에 "기술 제약 (필수)" 섹션으로 항상 명시 |

---

## #017 GitHub Pages report.html 항상 404

| 항목 | 내용 |
|------|------|
| **현상** | 이메일 카드의 `[전체 리포트 보기 →]` 링크 클릭 시 404 |
| **원인** | gh-pages 브랜치의 `.gitignore`에 `outputs/`가 포함되어 있었음. GA가 `peaceiris/actions-gh-pages`로 `./outputs`를 배포하려 해도 git이 해당 폴더를 무시 → 커밋에 포함 안 됨 → Pages에 파일 없음 |
| **해결** | gh-pages 브랜치 `.gitignore`에서 `outputs/` 라인 제거 후 기존 report.html 수동 배포 |
| **예방** | gh-pages 브랜치의 `.gitignore`는 main 브랜치와 다르게 관리. `outputs/`는 gh-pages에서 배포 대상이므로 절대 ignore하면 안 됨 |
| **관련 파일** | gh-pages 브랜치 `.gitignore`, `.github/workflows/daily-report.yml` |
| **발생일** | 2026-05-13 (발견 및 수정) |

---

## #018 gh-pages 브랜치 전환 시 untracked 파일 충돌

| 항목 | 내용 |
|------|------|
| **현상** | `git checkout gh-pages` 실행 시 `error: The following untracked working tree files would be overwritten by checkout` 로 전환 실패 |
| **원인** | gh-pages 브랜치에 추적되는 `.claude/settings.json` 등이 main 워킹 트리에 untracked 상태로 존재. git이 덮어쓸 수 없다고 거부 |
| **해결** | `git checkout -f gh-pages` (강제 전환) 으로 우회. 단 로컬 변경이 있으면 먼저 `git stash` |
| **예방** | 브랜치 전환 전 `git status`로 untracked 파일 확인. outputs/나 .claude/ 같은 경로가 두 브랜치에 동시 존재하지 않도록 관리 |
| **관련 파일** | gh-pages 브랜치 `.gitignore` |
| **발생일** | 2026-05-13 |

---

## #019 gh-pages에서 main의 소스코드를 직접 실행할 수 없음

| 항목 | 내용 |
|------|------|
| **현상** | gh-pages 브랜치로 전환 후 `node tools/desk/designer.js`를 실행하려 하면 상대 경로 임포트(`../utils/logger.js` 등)가 깨짐 |
| **원인** | gh-pages 브랜치는 정적 파일만 보유. `tools/` 디렉터리가 없거나 구버전임. main의 `designer.js`를 별도 파일로 추출해 실행해도 내부 import 경로가 엉킴 |
| **해결** | main에서 `git show gh-pages:outputs/.../data.json > 임시파일` 로 데이터를 뽑아온 뒤, main에서 빌드 → 생성된 HTML을 gh-pages에 커밋하는 방식 사용 |
| **예방** | 빌드는 반드시 main 브랜치에서 실행. gh-pages에는 빌드 결과물(HTML)만 커밋. 두 브랜치를 섞어서 빌드하려 하지 말 것 |
| **관련 파일** | `tools/desk/designer.js`, gh-pages 브랜치 `outputs/` |
| **발생일** | 2026-05-13 |

---

## #020 Gmail display:flex 레이아웃 붕괴 → 카드 너비 불균일

| 항목 | 내용 |
|------|------|
| **현상** | 브라우저에서는 카드 크기가 동일한데, Gmail에서 발송된 이메일을 보면 카드 너비가 제각각 |
| **원인** | Gmail은 `display:flex`와 `flex:1`을 불안정하게 처리. 콘텐츠 길이에 따라 카드 너비가 달라짐. 추가로 데이터 누락 시 행에 카드가 2개가 되면 `flex:1`이 그 행에서만 50%를 차지해 다른 행(3개)과 폭이 달라짐 |
| **해결** | `buildEmailCard` 내 모든 flex 레이아웃을 `<table><td width="50%">` / `<td width="33%">` 구조로 전환. 데이터 없는 칸은 빈 `<td>`로 자리를 유지 |
| **예방** | 이메일 HTML에서 `display:flex`는 사용하지 말 것. 이메일 레이아웃은 항상 `<table>` 기반으로 작성. 수정 후 반드시 Gmail 실발송으로 최종 확인 |
| **관련 파일** | `tools/desk/designer.js` (`buildEmailCard` 전체 — `cardRow2`, `cardRow3` 헬퍼 추가) |
| **발생일** | 2026-05-13 (발견 및 수정) |

---

## #021 코드 수정 후 git push 누락 → GA가 구버전으로 실행

| 항목 | 내용 |
|------|------|
| **현상** | 이메일에 과거 디자인 카드, 수급→시장강도 대체, VKOSPI→US VIX, 코인·컨센서스 섹션 누락 |
| **원인** | 14개 커밋이 로컬에만 있고 GitHub에 push되지 않았음. GitHub Actions는 원격 저장소(`origin/main`) 기준으로 코드를 실행하므로, 로컬 변경사항은 push 전까지 GA에 반영되지 않음 |
| **해결** | `git push origin main` 으로 14개 커밋 일괄 push |
| **예방** | **세션 종료 전 `git push origin main` 필수 실행** — CLAUDE.md 작업 컨텍스트 섹션에 강제 체크리스트로 추가. 이 단계를 빠뜨리면 다음날 GA가 당일 작업을 무시하고 구버전으로 리포트를 생성함 |
| **관련 파일** | `.github/workflows/daily-report.yml` (GA는 항상 origin/main 기준 실행), CLAUDE.md |
| **발생일** | 2026-05-14 (누적 기간: 2026-05-09 이후 14개 커밋) |

---

## #022 supply-collect.yml git add 묵묵히 실패 → 수급 데이터 항상 누락

| 항목 | 내용 |
|------|------|
| **현상** | GA 리포트에서 수급 섹션이 "시장강도"로 대체, VKOSPI가 "미국 VIX(참고)"로 표시됨 |
| **원인** | main `.gitignore`에 `outputs/` 한 줄이 있어 GA가 `supply.json`을 커밋 시도해도 git이 무시. `git add outputs/*/supply.json \|\| true`의 `\|\| true` 때문에 실패해도 에러 없이 조용히 넘어감 → `supply.json`이 GitHub에 단 한 번도 올라간 적 없음 |
| **해결** | `.gitignore`에서 `outputs/` → `outputs/**` 패턴으로 변경 후 `!outputs/`, `!outputs/*/`, `!outputs/*/supply.json` 예외 추가. Git은 부모 디렉토리가 완전 무시되면 하위 파일을 되살릴 수 없으므로 디렉토리 자체도 언-ignore해야 함 |
| **예방** | `.gitignore`에서 디렉토리 전체를 무시할 때 특정 하위 파일을 허용해야 한다면 `dir/**` + `!dir/` + `!dir/*/` + `!dir/*/file.json` 패턴을 써야 함. `dir/` 한 줄로는 부분 허용 불가 |
| **관련 파일** | `.gitignore`, `.github/workflows/supply-collect.yml` |
| **발생일** | 2026-05-14 (발견), 수급 누락 기간 불명 |

---

## #023 orchestrator.js→publisher.js TF 결과 단절 → GA 이메일 AI 요약 없음

| 항목 | 내용 |
|------|------|
| **현상** | GA가 보내는 이메일 카드에 AI 뉴스 요약(`top_stories`)과 시장 요약 섹션이 없음 |
| **원인** | `publisher.js`의 `buildEmailCard(data, {}, {}, reportUrl)` — TF 결과를 빈 객체 `{}`로 고정. 또한 `orchestrator.js`의 `publish()` 호출에 `tfResults`, `editorialPlan`, `reportUrl`이 전달되지 않아 이메일에 AI 분석이 전혀 반영되지 않음 |
| **해결** | `publisher.js`: 함수 시그니처에 `tfResults={}, editorialPlan={}` 추가 → `buildEmailCard(data, tfResults, editorialPlan, reportUrl)` 로 수정. `orchestrator.js`: `publish()` 호출 시 `reportUrl`, `tfResults`, `editorialPlan` 전달 |
| **예방** | `buildHtml`과 `buildEmailCard`는 같은 TF 결과를 받아야 HTML 리포트와 이메일 카드가 동일한 내용을 담음. 두 호출의 파라미터를 맞춰 쓸 것 |
| **관련 파일** | `tools/desk/publisher.js` (line 19, 48), `tools/orchestrator.js` (line 93) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #024 이메일 카드 뉴스 제목·요약 미흡 + 코인·애널리스트 섹션 누락

| 항목 | 내용 |
|------|------|
| **현상** | 이메일 카드 뉴스가 TF가 재작성한 제목으로 표시되고, 요약이 한 줄(`market_impact`)뿐. 코인·애널리스트 섹션 없음 |
| **원인 1** | `buildEmailCard`의 newsItems 매핑이 `f.headline`(TF 재작성 제목)을 사용. rawNews URL 매칭 없음 |
| **원인 2** | `tf_news.js` findings에 bullet 요약 필드(`summary`) 없음 — `market_impact` 한 줄만 있었음 |
| **원인 3** | `main.js`가 crypto/DART를 수집하지 않아 `data.json`에 `crypto: null, dart: null` → 로컬 이메일에 코인 미표시 |
| **원인 4** | `preview_send.js`에 `runTFCrypto` 호출 없음. `buildEmailCard`에 analyst TF 결과 미전달 |
| **해결** | `tf_news.js`: findings에 `summary`(2-3 bullet 배열) 필드 추가. `designer.js` `buildEmailCard`: source_url로 rawNews 매칭해 원문 제목 사용, bullet 요약 렌더링, 애널리스트 섹션 추가. `main.js`: `collectCrypto`/`collectDart` 추가 + validateData 후 data에 머지. `preview_send.js`: `runTFCrypto` import 및 병렬 실행, 모든 TF 결과 buildEmailCard에 전달 |
| **예방** | 이메일 카드에 새 TF 팀 결과를 추가할 때 반드시 3곳 동시 수정: ①수집(main.js) ②TF 실행(preview_send.js) ③렌더(designer.js buildEmailCard) |
| **관련 파일** | `tools/teams/tf_news.js`, `tools/desk/designer.js` (buildEmailCard), `tools/main.js`, `tools/preview_send.js` |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #025 Gemini 503 → 모든 비고 열 + KOSPI 이슈 열 빈값 (AI 의존 단일 장애점)

| 항목 | 내용 |
|------|------|
| **현상** | 리포트 국내증시·해외증시·환율·원자재 섹션의 **비고** 열이 전부 빈 값. KOSPI 5거래일 추이의 **주요 이슈** 열도 빈 값 |
| **원인** | `_buildRowNotes()`와 `_buildHistIssues()` 모두 Gemini 1회 호출 후 실패하면 `{}` 반환. 503(Service Unavailable)은 간헐적으로 발생해 폴백이 없으면 매번 빈 값 |
| **해결** | 두 함수 모두 **데이터 기반 폴백 먼저 계산 후 Gemini로 보완**하는 방식으로 변경. `_buildRowNotesFallback(d, o, fx, c)` 신규 함수 추가 → 방향 기호·변동률·환율 레벨·VKOSPI 임계값 라벨·수급 요약을 수치에서 직접 생성. Gemini 성공 시 결과로 override, 실패 시 폴백 그대로 반환 |
| **예방** | AI에 의존하는 컬럼 생성 함수는 반드시 AI-free 폴백을 먼저 만들어야 함. 폴백이 있어야 Gemini가 503이어도 최소 정보는 표시됨 |
| **관련 파일** | `tools/desk/designer.js` (`_buildRowNotesFallback`, `_buildRowNotes`, `_buildHistIssues`) |
| **발생일** | 2026-05-14 (반복 발생) |

---

## #026 코인 표 3가지 오류 (순위 하드코딩, 시장동향 빈값, 컬럼 비율 부적합)

| 항목 | 내용 |
|------|------|
| **현상** | ① BTC·ETH 순위 열에 `—` 표시 ② ETH·기타 코인 시장동향 열 빈값, BTC만 25자 ③ 표 컬럼 너비(6%/14%/24%/18%/38%)가 컨텐츠와 맞지 않음 |
| **원인** | ① BTC·ETH 행에 `<td>—</td>` 하드코딩 — `top10` 배열의 `rank` 필드를 조회하지 않음 ② `tfCrypto.market_summary.slice(0,25)` BTC 1건만, ETH와 filteredTop10은 빈 문자열 ③ 시장동향 열이 실제로 긴 텍스트를 표시하게 됐는데 너비(38%)가 좁음 |
| **해결** | ① `top10.find(c => c.symbol === 'BTC').rank` 등으로 실제 순위 조회 ② `tfCrypto.findings`에서 `asset` 매칭으로 코인별 `key_level` 추출 ③ 컬럼 비율 `5%/10%/22%/15%/48%`로 조정 |
| **예방** | 하드코딩된 `—`는 데이터 구조 변경 시 즉시 버그가 됨. 항상 실제 데이터에서 조회. 신규 열 추가 시 내용 길이를 고려해 `width` 재조정 |
| **관련 파일** | `tools/desk/designer.js` (`_buildCryptoSection`) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #027 Gemini 503 → 애널리스트 섹션 완전 누락

| 항목 | 내용 |
|------|------|
| **현상** | DART에 분석 대상 리포트가 있어도 HTML 리포트의 **애널리스트 리포트** 섹션 자체가 사라짐 |
| **원인** | `include_analyst = tfAnalystResult.findings?.length > 0` — Gemini 503으로 tf_analyst가 `findings: []` 반환 → `include_analyst = false` → `_buildAnalystSection()` 호출 자체를 건너뜀 |
| **해결** | `include_analyst = data.dart?.reports?.length > 0` 으로 변경 (DART 데이터 유무 기준). Gemini 실패 시에는 `data.dart.reports`를 직접 `findings` 형식으로 변환하는 DART 폴백 추가 (`company = corp_name`, `key_thesis = report_nm`) |
| **예방** | 섹션 표시 여부는 **AI 분석 결과**가 아닌 **원시 데이터 존재 여부**로 결정할 것. AI는 내용을 풍부하게 하는 역할이지 섹션을 켜고 끄는 조건이 돼서는 안 됨 |
| **관련 파일** | `tools/preview_send.js` (line ~174, `include_analyst`), `tools/desk/designer.js` (`_buildAnalystSection`) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #028 VKOSPI Naver 409 영구 차단 → investing.com 스트리밍 파싱 전환

| 항목 | 내용 |
|------|------|
| **현상** | `domestic.vkospi.today = null` 지속, 리포트에 "VKOSPI (전일값)"으로 표시 또는 누락 |
| **원인** | `m.stock.naver.com/api/index/VKOSPI/basic`가 VKOSPI 심볼에 대해 409 영구 반환. 전일값 carry-forward가 폴백으로 설정돼 있었으나 아예 없는 날은 null |
| **이전 잘못된 대응** | US VIX(`^VIX`)로 대체 표시 — 사주 지적: "VKOSPI 못찾으면 방법을 강구해야지, 대안으로 계속 주면 안 된다" |
| **해결** | `kr.investing.com/indices/kospi-volatility` 스트리밍(responseType: 'stream') 파싱으로 교체. 189KB까지만 읽고 `data-test="instrument-price-last"`, `data-test="instrument-price-change"` 셀렉터로 값 추출. VIX 폴백 완전 제거 |
| **예방** | 특정 API가 막히면 대안 소스를 찾아야 함. 다른 자산의 대리 지표를 제공하는 것은 정보 왜곡이므로 금지 |
| **관련 파일** | `tools/collectors/domestic.js` (VKOSPI 수집 로직 전체) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #029 supply-collect.yml 크론 중복 제거

| 항목 | 내용 |
|------|------|
| **현상** | 16:40 KST 별도 GitHub Actions(`supply-collect.yml`)로 수급 데이터를 수집하고 `supply.json`을 저장했지만, 매일 발송 전 `preview_send.js`가 이미 `investorDealTrendDay.naver`에서 5거래일 수급 히스토리를 직접 수집하는 구조로 변경됨 |
| **원인 (중복)** | `supply-collect.yml` 16:40 실행 → `supply.json` 저장 → `main.js` STEP 2.5에서 로드 → `preview_send.js`도 별도 수집. 두 경로가 동시에 존재해 복잡도 증가 |
| **해결** | `supply-collect.yml` 삭제, `main.js` STEP 2.5 제거, `.gitignore`에서 `supply.json` 예외 규칙 제거. `preview_send.js`의 `fetchSupplyHistory()` 단일 경로로 통일 |
| **예방** | 데이터 수집 경로가 2개 이상이면 항상 단일화할 것. 중복 파이프라인은 유지 비용만 높이고 결과는 같음 |
| **관련 파일** | `.github/workflows/supply-collect.yml`(삭제), `tools/main.js`(STEP 2.5 제거), `.gitignore` |
| **발생일** | 2026-05-14 (정리) |

---

## #030 `include_analyst` 조건 오류 → 애널리스트 섹션 재누락

| 항목 | 내용 |
|------|------|
| **현상** | tf_analyst가 뉴스 기반으로 3건 findings를 생성했는데 HTML 리포트에 애널리스트 섹션이 없음 |
| **원인** | #027 수정 시 `include_analyst = tfAnalystResult.findings?.length > 0` → `data.dart?.reports?.length > 0`으로 교체함. DART 0건인 날 findings가 있어도 조건이 false → 섹션 미표시 |
| **해결** | `findings?.length > 0` 조건으로 복원. DART 폴백은 별도 로직으로 분리 |
| **예방** | **섹션 표시 여부는 최종 AI 결과(findings)로만 판단**. Gemini 실패 대책은 "재시도" 또는 "DART 원본 폴백"이지 "표시 조건 변경"이 아님. 조건 수정 시 "DART 0건이지만 뉴스 기반 findings가 있는 경우"를 반드시 테스트 케이스로 확인 |
| **관련 파일** | `tools/preview_send.js` (`include_analyst` 조건) |
| **발생일** | 2026-05-14 (수정 직후 재발) |

---

## #031 수급추이 표 간격 부족 + 제목 스타일 미흡

| 항목 | 내용 |
|------|------|
| **현상** | KOSPI 수급 추이 표가 바로 위 지수 표에 바짝 붙어있어 별도 섹션임을 인식하기 어려움. 제목도 소문자 `.st` 스타일이라 섹션 제목처럼 보이지 않음 |
| **원인** | `_buildSupplyHistory()` 반환 div에 `margin-top:4px`만 있었음. 제목을 `.st` (소형 부제목) 클래스로만 지정 |
| **해결** | `margin-top:20px; padding-top:16px; border-top:1px solid #e8eaed` 추가해 시각적 구분. 제목 font-size 13px font-weight 700로 상향 |
| **예방** | 표 내부에 서브섹션을 추가할 때는 반드시 `border-top` + `margin-top:16px 이상` + 본문 `.sec-title`에 준하는 폰트 크기로 제목 설정 |
| **관련 파일** | `tools/desk/designer.js` (`_buildSupplyHistory`) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #032 애널리스트 Gemini 503 시 섹션 자체 소실 — 재시도 없음

| 항목 | 내용 |
|------|------|
| **현상** | Gemini 503이 뜨면 tf_analyst findings = [] → include_analyst = false → 섹션 완전 소실 |
| **원인** | tf_analyst.js가 내부에서 503을 catch하고 `{ findings: [] }` 반환. 호출부에서 재시도 없이 바로 다음 단계로 진행 |
| **해결** | `preview_send.js`에 재시도 로직 추가: findings 빈 경우 애널리스트 뉴스 존재 여부 확인 후 3초 대기 후 1회 재호출. 재시도 성공 시 findings로 교체 |
| **예방** | Gemini 호출이 실패해 결과가 빈 경우 재시도 1회는 표준 패턴으로 적용. 특히 섹션 표시 여부를 결정하는 핵심 TF팀(analyst, news)은 재시도 필수 |
| **관련 파일** | `tools/preview_send.js` (TF-2 병렬 실행 블록 이후) |
| **발생일** | 2026-05-14 (발견 및 수정) |

---

## #033 ⭐ GA 진입점 문서-실제 불일치 — 어제 패치 60%가 GA에 반영 안 됨

| 항목 | 내용 |
|------|------|
| **현상** | 2026-05-15 메일 리포트에서 KOSPI/KOSDAQ N/A, 애널리스트 섹션 누락, "코스피 7981선" 헤드라인이 실제 5거래일 추이(7,844.01)와 불일치. 어제(05-14) 수정한 12개 패치 중 5개(`preview_send.js`·`main.js` 한정 변경)가 전혀 작동하지 않음 |
| **원인** | 2026-05-13 커밋 `32c8f8b` 에서 GA 워크플로우가 `node tools/orchestrator.js --now` 단독 실행으로 전환됐는데, **CLAUDE.md와 에이전트 정의 파일은 여전히 "main.js·preview_send.js가 GA 진입점"이라고 안내**. 편집장(Claude)이 잘못된 문서를 신뢰하고 deprecated 파일들에 새 기능(애널리스트 재시도, DART 폴백, --force 플래그, include_analyst 조건)을 넣음. GA는 그 코드를 호출하지 않음 |
| **해결** | (1) `tools/orchestrator.js` Layer 2 직후에 애널리스트 Gemini 재시도·DART 폴백·DART URL 매칭 이식. (2) `tools/pipeline/index.js _applyLiveFallbacks` 에 KOSPI/KOSDAQ 종가 폴백 추가 — Yahoo `^KS11` 실패해도 Naver m.stock으로 보완. (3) `tools/desk/editor.js` 에 헤드라인 KOSPI 수치 검증 추가 — 실제 데이터와 200p/2% 이상 어긋나면 정정. (4) CLAUDE.md·에이전트 정의 정정 — orchestrator가 GA 단독 진입점임을 모든 문서에 명시. (5) `tools/main.js`·`tools/preview_send.js` 상단에 DEPRECATED 헤더 추가 |
| **예방** | **코드 변경 전 반드시 `.github/workflows/daily-report.yml` 의 `run:` 라인을 먼저 확인.** "이 변경이 GA의 호출 경로에 들어가는가?"를 묻고, 답이 No면 작업 중단. orchestrator 경로: `tools/pipeline/*`, `tools/teams/*`, `tools/desk/*`. 그 외 진입점 파일(main.js, preview_send.js) 수정은 로컬 검증 한정 |
| **관련 파일** | `tools/orchestrator.js`, `tools/pipeline/index.js`, `tools/desk/editor.js`, `CLAUDE.md`, `.claude/agents/desk.md`, `.claude/agents/orchestrator.md` |
| **발생일** | 2026-05-14 (작업) → 2026-05-15 (메일 수신 후 발견 및 수정) |

---

## #034 ⭐ 휴장일 분기 함정 — VKOSPI·시가총액·수급·5거래일추이 통째 누락

| 항목 | 내용 |
|------|------|
| **현상** | 휴장일(2026-05-15 토) GA 발송 메일에서 국내 증시 표의 VKOSPI 행, 시가총액 행, 수급 행, 5거래일 수급추이 그래프 4개가 모두 누락 |
| **원인** | (1) `collectors/domestic.js` 첫 줄이 `isHoliday=true`면 `{ kospi:null, kosdaq:null, supply:null, kospiHistory:[], isHoliday:true }`만 반환하고 종료 → VKOSPI·marketCap 수집 시도조차 안 함. (2) `validators/data_validator.js`가 `isHoliday=true`면 `{ isHoliday:true }`만 통과시켜 폴백으로 채워도 designer까지 못 감. (3) 5거래일 수급추이 수집 함수(`fetchSupplyHistory`)는 deprecated `preview_send.js`에만 있고 orchestrator는 호출 안 함 — 오답노트 #033과 같은 패턴 |
| **해결** | (1) validator의 isHoliday 분기 제거 — 휴장일도 모든 필드 그대로 통과 (필드 자체는 null일 수 있음). (2) `pipeline/index.js _applyLiveFallbacks`에 시가총액(`fetchKospiMarketCap` 호출), VKOSPI(`investing.com` 폴백), 당일 수급 + 5거래일 추이(`investorDealTrendDay` 스크래핑) 모두 추가. (3) `domestic.js`에서 `fetchKospiMarketCap` export해 pipeline에서도 호출 가능 |
| **예방** | 휴장일 분기 코드 추가 시 반드시 "이 분기가 다운스트림에 어떤 필드를 누락시키는가" 점검. validator는 가능한 한 모든 필드를 통과시키고 표시 여부는 designer 책임으로. 새 기능 추가는 무조건 orchestrator 호출 경로(pipeline·teams·desk)에. preview_send.js·main.js는 정말 더 이상 만지지 말 것 |
| **관련 파일** | `tools/pipeline/index.js`, `tools/validators/data_validator.js`, `tools/collectors/domestic.js` |
| **발생일** | 2026-05-15 (휴장일 GA 메일 검증 후 발견 및 수정) |

---

## #035 ⭐ "서버 일시 장애" 오진 — 사실은 외부 API 리디자인 + 봇 차단 강화

| 항목 | 내용 |
|------|------|
| **현상** | 2026-05-15 6차 작업 단독 검증에서 한경 컨센서스 엔드포인트가 500 반환. 당시 한경 메인까지 500이라 "한경 측 일시 장애"로 판단하고 GA 다음 실행 자동 복구를 기대 |
| **원인 (실제)** | 한경 측 일시 장애가 **아니었다**. 두 가지 변화가 동시에 일어남: (1) URL 리디자인 — `/apps.analysis/analysis.list` → `/analysis/list`. 옛 URL은 영구 404. (2) 봇 차단 강화 — 짧은 User-Agent(`Mozilla/5.0`)는 500으로 거부, 풀 브라우저 UA + `Accept: text/html...` 헤더가 있어야 200 응답 |
| **해결** | (1) `tf_analyst.fetchHankyungConsensus`의 URL을 `/analysis/list`로 변경, 파라미터도 `category=CO`→`report_type=CO`, sdate/edate를 `YYYYMMDD`→`YYYY-MM-DD`로 갱신. (2) UA를 풀 Chrome UA로 + `Accept`·`Accept-Language` 헤더 추가. (3) HTML 구조 변경에 맞춰 파서 재작성 — 회사명·종목코드가 제목 안에 합쳐졌으므로 `^(.+?)\s*\((\d{6})\)\s*(.*)$` 정규식으로 분리. target_price·rating 필드도 신규 추출. (4) `orchestrator.js`의 최종 안전망 검색 URL(`apps.analysis/analysis.list?search_value=…`)도 신규 URL로 교체 |
| **예방** | **외부 API가 죽었을 때 "서버 일시 장애"라고 단정 짓기 전에 반드시 1차 확인**: ① 다른 경로(메인 도메인 등)는 200인가? ② 옛 URL과 신규 URL 후보(점 → 슬래시 등 흔한 패턴)를 둘 다 찔러본다 ③ 브라우저 풀 UA + Accept 헤더로 재시도 — 봇 차단인지 진짜 장애인지 구별 가능. 외부 API가 우리 코드의 1차 소스라면 외부 변경에 빠르게 적응할 수 있도록 모니터링 로그를 강화해야 함 (수집 0건일 때 별도 alert) |
| **관련 파일** | `tools/teams/tf_analyst.js`, `tools/orchestrator.js` |
| **발생일** | 2026-05-15 (오진) → 2026-05-16 (사주 지시 "1번부터 가자"로 재점검하여 발견·수정) |

---

*최종 업데이트: 2026-05-16 (v14 — #035 추가)*

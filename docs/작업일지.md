# 작업일지 — 일일 시장 리포트

> 날짜별 작업 내용을 기록. "오늘 무엇을 했나"를 다음 세션에서 빠르게 파악하기 위한 문서.
> 오류·실수 기록은 `오답노트.md` 참조.

---

## 📋 다음 세션 To-Do (바로 실행 가능, 우선순위 순)

세션 시작 시 위에서부터 읽고 진행하면 된다. 각 항목에 **상태·예상 시간·관련 파일** 표시.

### 0순위 — 매일 첫 점검 (≈ 5분)

- [ ] **GA 실행 결과 확인** — 2026-05-17 08:00 KST 정기 발송이 정상 동작했는지
  - gh-pages 최근 commit 시각·내용 확인: `git fetch origin gh-pages && git log origin/gh-pages --format="%h %ai %s" -3`
  - 사주가 받은 메일 시각 디자인 평가 (헤드라인 임팩트·풋터·표 너비·핵심 수치 3카드)
  - 어제(2026-05-16) push된 모든 디자인 변경이 메일·gh-pages에 정확히 반영됐는지

### 1순위 — Phase 2: feeds shared 이동 (≈ 1시간)

> 사주 비전 "콘텐츠 미디어 플랫폼"의 다음 토대. 여러 콘텐츠가 같은 feed 공유.

- [ ] `tools/layer-2-research/tf-analyst/feeds/dart_feed.js` → `tools/shared/feeds/dart.js` 이동
- [ ] `tools/layer-2-research/tf-crypto/feeds/crypto_feed.js` → `tools/shared/feeds/coingecko.js` 이동
- [ ] `tools/layer-2-research/tf-news/feeds/news_feed.js` → `tools/shared/feeds/naver-news.js` 이동
- [ ] `tf-analyst/index.js`의 `fetchHankyungConsensus()` 함수를 `tools/shared/feeds/hankyung-consensus.js`로 분리
- [ ] 각 TF팀의 import 경로 갱신 (`./feeds/...` → `../../shared/feeds/...`)
- [ ] `tools/shared/feeds/README.md` Phase 2 항목 "✅ 완료"로 갱신
- [ ] `CLAUDE.md` 핵심 파일 참조 트리 갱신
- [ ] 검증: `node --check` + `orchestrator --now --preview --skip-collect`

**관련 파일**: `tools/shared/feeds/`, `tools/layer-2-research/*/index.js`

### 2순위 — pipeline 에이전트 정의 파일 정정 (≈ 5분)

- [ ] `.claude/agents/pipeline.md`의 "tools/layer-1-pipeline/collectors/ — 레거시 수집기"라는 문구 정정 (폴더 재편 후 정식 Layer 1 소속)

### 3순위 — Phase 3 시작 (사주 결정 시)

> 사주가 "이 콘텐츠 만들자"라고 결정해야 시작. 결재 후:

- [ ] **tf-equity 에이전트** (≈ 3시간) — 개별 기업 심층 분석
  - `tools/layer-2-research/tf-equity/` 신설 — index·feeds(DART 재무·한경 추가 컨센서스)
  - 사주가 회사 1개 지정 시 분석 또는 매일 KOSPI 시총 상위 1개 자동 분석
  - 결과: 회사 개요·재무 트렌드·애널리스트 종합·핵심 리스크
- [ ] **card-news 콘텐츠** (≈ 2~3시간) — Instagram·SNS용 카드 PNG
  - `tools/contents/card-news.js` + `tools/layer-3-desk/design/card-news.js`
  - Puppeteer 또는 sharp로 HTML → PNG
  - 발행 채널 추가: `tools/layer-3-desk/publisher/channels/instagram.js`
  - design 다크 모드 색 매핑 완성 (현재 placeholder)
- [ ] **decision 에이전트** (≈ 2시간) — 투자 의향도 추적
  - Notion DB 컬럼 추가 ("의향: 긍정/중립/부정", "타임라인")
  - 사주가 Notion에서 표시 → 시스템이 1개월 후 실제 주가 변화와 매칭해 정확도 측정
  - ⚠️ **보안 점검 필수**: 사주 투자 의향은 개인 정보 → gh-pages 공개 X, Notion 비공개 페이지만

### 4순위 — 미처리 보완점 (긴급도 낮음)

- [ ] **publisher 통합 다이제스트 모드** — 콘텐츠 N개 → 메일 1통으로 묶기 (Phase 3 콘텐츠 늘기 전에)
- [ ] **design 폰트 임베드** — Pretendard 웹폰트 임베드 (메일 호환성 트레이드오프 검토)
- [ ] **pipeline 10거래일 그래프** — 현재 5거래일에서 확장 (사주 흐름 추적용)
- [ ] **tf-news 출처 다양화** — Naver Search 단일 의존 → 추가 소스
- [ ] **tf-analyst 다중 컨센서스 소스** — 와이즈리포트·이베스트 등 (한경 폴백 강화)
- [ ] **tf-crypto 온체인 지표 강화** — 거래소 보유량·CEX/DEX 유출입 등 (현재 시세·심리만)
- [ ] **editor 콘텐츠별 분기** — Phase 3 새 콘텐츠 추가 시 자연 발생

### 5순위 — 시스템 안정성

- [ ] **metrics 활용** — `outputs/{date}/metrics.json` 누적해 주 1회 헬스 체크 리포트 자동 생성
- [ ] **외부 API 변경 모니터링** — 한경 같은 외부 사이트가 또 리디자인하면 즉시 감지 (수집 0건이 N일 연속이면 alert)

### 🔖 세션 시작 표준 체크리스트

1. `docs/작업일지.md` 위 To-Do 0순위 먼저 → 어제 GA 결과 확인
2. `docs/오답노트.md` 최근 항목(#036~#039) 숙지 — 디자인 위임 사이클·한국어 조사·SendMessage 부재·메일 폭주
3. 사주의 우선순위 질문 — 1·3·4순위 중 어느 것부터?
4. 작업 시작 전 사주 결재 (1·2순위는 결재 불필요, 3순위 이상은 사주 결정 필요)

---

## 2026-05-16 (계속)

### 20차 작업 — design 에이전트: 5차 최종 micro 보정 (글씨 위계·정렬·tabular-nums 일관성)

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **`.ntbl th` tabular-nums 누락 추가**: `.tbl th`에는 `font-variant-numeric:tabular-nums`가 있었으나 `.ntbl th`에는 없어 뉴스 표 헤더만 예외였음. 추가해 두 표 헤더 일관화.
- **`.td-cat` tabular-nums 추가**: 옆 열 `.td-date`는 `font-variant-numeric:tabular-nums`가 있었으나 `.td-cat`에는 없었음. 동일 열 위계이므로 추가.
- **`.hdr-date-sub` letter-spacing 조정**: 헤더 우측 날짜 메타 텍스트가 `0.08em`으로 같은 "uppercase 라벨" 위계인 `.hdr-eng`(0.15em)·`.footer-label`(0.08em)·헤드라인 라벨(0.15em) 사이에서 어색했음. `0.10em`으로 조정해 footer-label과 hdr-eng 사이 자연스러운 위계 형성.
- **구조·색·기능 변경 없음**: 폰트 크기·패딩·margin·색·박스·섹션 전혀 미변경.
- **문법 검증**: 직독 전수 대조 — 오류 없음.

### 19차 작업 — design 에이전트: 4차 보완 (헤드라인 임팩트 + 풋터 + 차트 색 + 메타 정보)

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **A. 헤드라인 임팩트**: `.headline-block` 신설(border-left:4px solid #1e3a8a), `.headline-label`(10px uppercase letter-spacing 0.15em, color:#374151), `.headline-text`(28px Bold, #0f172a, line-height:1.3). buildHtml·buildEmailCard 양쪽: `TODAY'S HEADLINE` 레이블 + 28px/22px(메일) 헤드라인 텍스트. 상충 알림 박스는 헤드라인 직후 유지.
- **B. 풋터/면책 강화**: 리포트 HTML — 기존 divider+note 제거, `.report-footer` 섹션 신설(background:#fafafa, border-top:1px solid, padding:20px 24px). 출처·발행정보·면책 3블록. 메일 카드 — CTA 버튼 별도 div 분리 후 아래 동일 3블록 인라인 스타일 풋터 추가.
- **C. 차트 색 통일**: `_buildChartScript` — KOSPI 선/점 `#E24B4A`→`#1e3a8a`, 거래대금 막대 `rgba(55,138,221)`→`rgba(55,65,81)`(차콜), 격자선 `#e5e7eb`, 축 라벨 `#475569`. `_buildChartUrl` quickchart 폴백도 동일.
- **D. 메타 정보 정돈**: `buildHtml` — `EN_DAYS·reportDow·reportDatePrimary·reportDateSub` 추가. `.hdr` CSS: flex + align-items:flex-end + justify-content:space-between + border-bottom:2px solid. 좌측 영문(11px uppercase)+한글(16px Bold), 우측 날짜(13px Bold)+요일·KST(10px uppercase). `buildEmailCard`도 `EN_DAYS_EMAIL·emailDow·emailDatePrimary·emailDateSub` 동일 적용.
- **문법 검증**: 직독. 백틱·중괄호·template literal 전수 대조 — 오류 없음.

### 18차 작업 — desk 에이전트: AI Summary + sectionSummaries 풍부화

**수정 파일**: `tools/layer-3-desk/editor/index.js`, `tools/layer-3-desk/editor/README.md`

- **AI Summary 불릿 강화**: 4~6개·50자 이내 단답형 → 5~7개·60~90자 완결 문장. 증권사 데일리 시황 톤 지정. Gemini 프롬프트에 KOSPI/KOSDAQ/VKOSPI·수급·5거래일추이·해외·환율·원자재·코인·테마 전 데이터 입력. 구성 순서(큰그림→국내→해외→환율·금리→코인→테마·위험) 명시.
- **Gemini 503 폴백 강화**: 빈 배열 반환 → `_buildFallbackBullets()` 신설. sectionSummaries 5개 첫 문장 발췌 + 큰그림 불릿 조립으로 5~7개 결정론적 불릿 생성. 빈 메일 발송 방지.
- **domestic Summary 보강**: VKOSPI 추가(변동성 의미 해석), 5거래일 외국인 수급 추이(`_buildSupplyTrendContext()`) 추가("외국인 N거래일 연속 순매도 (합산 X조원)").
- **overseas Summary 보강**: SOX 항상 포함(변동률 무관), 미국 약세 → 한국 동조 부담 해석 추가.
- **fxRates Summary 보강**: us10y bp 변화 강조, 10bp 이상 급등 시 "강달러 압력 → 외국인 매도" 메커니즘 설명 추가. 달러·금리 동반 변동 시 신흥시장 부담 해석 추가.
- **commodities Summary 보강**: 안전자산(금) vs 위험자산(원유) 동반하락 시 "디레버리징" 해석 추가. 니켈·백금 추가.
- **crypto Summary 보강**: 공포탐욕지수 구간별 의미 해석(극단공포/공포/탐욕/극단탐욕) 추가. 김치프리미엄 역프리미엄·정상·고프리미엄 해석 추가. TF 시그널 문장으로 삽입.
- **헬퍼 신설**: `_amtStr()`(억→조 변환), `_buildSupplyTrendContext()`(수급 추이), `_buildHistoryContext()`(KOSPI 5거래일 방향성).
- **실행 순서 변경**: sectionSummaries를 Gemini 호출 전에 먼저 생성 → 폴백 시 즉시 활용 가능.
- **문법 검증**: 파일 직독, 백틱·중괄호·함수 닫힘·template literal 전수 대조 — 오류 없음.

### 17차 작업 — design 에이전트: 코인 중복 제거 + 전 표 컬럼 너비 정돈

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **코인 섹션 중복 제거**: `_buildCryptoSection()` 말미의 `tfCrypto.market_summary` 박스(border-left:3px solid #0e7490) 라인 제거. `sectionSummaries.crypto`(secSummary) Summary 박스가 유일한 코인 개요 출력으로 단일화. `buildEmailCard`의 코인 섹션은 이미 이전 위임에서 제거돼 있어 추가 수정 불필요.
- **전 표 `table-layout:fixed` 적용**: `.tbl`, `.ntbl` CSS 정의에 `table-layout:fixed` 추가.
- **`.tbl td` 기본 ellipsis 처리**: `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` 추가. 애널리스트 `.bi` 열·뉴스 `.td-ttl`·`.td-sum`은 줄바꿈 허용으로 개별 복원.
- **국내 증시 표**: 구분 34% / 당일·전일 각 20% / 변동 26%.
- **KOSPI 추이 표**: 날짜 18% / 종가 14% / 전일比 13% / 등락률 10% / 거래대금 14% / 이슈 31%. 기존 colgroup 제거.
- **해외 증시 표**: 구분 34% / 전일·전전일 각 20% / 변동 26%.
- **환율·금리 표**: 구분 38% / 당일·전일 각 20% / 변동 22%.
- **원자재 표**: 구분 35% / 당일·전일 각 21% / 변동 23%.
- **코인 표 (인라인 TH)**: 순위 12% / 심볼 23% / 시세 35% / 24h 변동 30%.
- **애널리스트 표**: 종목 22% / 증권사 14% / 의견 10% / 목표가 14% (`.tbl th.r` 우측 정렬 신설) / 핵심논거 40%.
- **뉴스 표**: 일자 70px / 구분 64px / 제목 30% / 요약 나머지(% 미지정).
- **문법 검증**: 직독. 백틱·중괄호·template literal 전수 대조 — 오류 없음.

### 16차 작업 — design 에이전트: 사주 인터뷰 2차 — 모바일 다이제스트 적용

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **리포트 HTML 모바일 카드형 변환**: `@media(max-width:600px)` 확장. `.tbl thead{display:none}`, `.tbl tbody tr{display:block;border:1px solid var(--border-strong);border-radius:6px;margin-bottom:8px}`, 1열(종목명) 15px Bold 섹션 액센트 색, 2·3열(당일·전일) 14px, 5열+ 11px 서브 텍스트. `.ntbl-outer`로 뉴스 표 가로 스크롤 분리.
- **메일 카드 다이제스트화 — 제거 섹션**: mktSection(증시 2×2), fxSection(환율·원자재), coinSection(코인), analystSection(애널리스트) 전부 제거.
- **핵심 수치 3카드 신규**: KOSPI·S&P500·BTC 고정. `_make3Card()` 헬퍼로 bg/border/색 방향별 분기. 휴장일 KOSPI → 전일 종가 + 'KOSPI (휴장)' 라벨, 데이터 누락 → '—' 표시. `<table>` 기반 3열 배치(width:33%).
- **뉴스 3건 다이제스트**: 기존 6건 → 3건. 중요도 내림차순, 테마 중복 방지 로직 유지.
- **알림 박스 신규**: 김치프리미엄(BTC·ETH 한 줄) + 목표가 변동(최대 4건 + 총 N건) 두 줄. 데이터 없으면 자동 미표시.
- **헤드라인 박스**: 헤더 바로 아래 `editorialPlan.headline` 표시. 없으면 생략.
- **조립 순서**: 헤더 → 헤드라인 → 상충/Summary → 3카드 → 뉴스3건 → 알림박스 → CTA.
- **문법 검증**: template literal 백틱·중괄호·변수명(`dir`/`kospiDir`/`btcDir` 충돌 없음) 직독 전수 대조. 오류 없음.

### 15차 작업 — design 에이전트: 사주 인터뷰 기반 증권사 PDF 격식 전면 재구축

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **베이스 톤**: 순백(#ffffff) + 잉크 검정(#0f172a). 기존 회색 --bg-page:#f4f5f7 → #ffffff 교체.
- **섹션별 액센트 5종**: --accent-domestic:#1e3a8a(딥네이비) / --accent-overseas:#374151(차콜) / --accent-fxrates:#4338ca(인디고) / --accent-commodities:#854d0e(어스브론즈) / --accent-crypto:#0e7490(딥티얼). 섹션 헤더·th 배경·Summary 박스에 각각 적용.
- **표 그리드**: 모든 td/th에 `border:1px solid var(--border-strong)`. 기존 border-bottom만 → 전체 1px 경계. 섹션별 th 배경 rgba 4% + 섹션 액센트 색 텍스트.
- **섹션 제목 듀얼 표기**: `.sec-eng`(11px uppercase letter-spacing 0.15em 섹션 액센트) + `.sec-kor`(16px Bold 잉크 검정). 7개 섹션 전부 영문 매핑 적용.
- **micro-typography**: tabular-nums 숫자 컬럼, letter-spacing 0.08em th, 헤드라인 21px Bold letter-spacing -0.01em.
- **새 데이터 박스 3개**: 김치프리미엄(코인 섹션 아래 .kimchi-box), 목표가변동(애널리스트 표 위 .target-box, 최대 3건), 상충알림(헤드라인 바로 아래 .conflict-box). null/빈 배열이면 자동 숨김.
- **buildEmailCard 동일 톤 적용**: secHdr() 시그니처 변경(engLabel·korLabel·meta·accentColor), CSS variables 대신 직접 색 값, 동일 3개 박스 인라인 스타일로 구현.
- 다크 모드 변수 블록 유지 (Phase 3 활성화용). 문법 검증: 직독으로 백틱·중괄호·인라인 스타일 전수 대조, 오류 없음.

### 14차 작업 — design 에이전트: 색상·톤 세련화 + 표 여백 조정 + 모바일 최적화 + 다크 모드 옵션

**수정 파일**: `tools/layer-3-desk/design/index.js`, `tools/layer-3-desk/design/README.md`

- **색상·톤 세련화**: 저채도+깊은 콘트라스트 방향. 텍스트 1차 `#111827`, 2차 `#4b5563`, 액센트 `#1e40af`(딥 인디고). CSS variables `--color-th-bg`, `--color-th-border`, `--color-accent`, `--bg-page`, `--bg-card` 신설. 한국 증시 빨강·파랑 절대 유지. `buildEmailCard` 카드 경계 색·label 스타일도 동일 팔레트로 정렬.
- **표 여백 조정**: `.tbl th` 패딩 7→5px, `.tbl td` 패딩 6→4px. th 폰트 11→10px uppercase letter-spacing. td 폰트 13→12px. 코인 섹션 인라인 TH/TD 동일 기준 적용. `buildEmailCard` 섹션 패딩 24→22px, 카드 min-height 88→84px, 패딩 14→12px.
- **모바일 최적화**: `@media(max-width:600px)` 보강 — body padding 8×6px, wrap padding 14×12px·shadow 제거, 표 `min-width:460px` + `overflow-x:auto` 가로 스크롤, `.td-sum` 숨김 유지, 수급 카드 단열, 차트 160px.
- **다크 모드 옵션**: `editorialPlan.theme==='dark'`이면 `<html class="theme-dark">`. `.theme-dark{}` CSS 블록에 `--bg-page:#0b0f1a`, `--bg-card:#131929`, `--color-accent:#60a5fa` 등 다크 팔레트 정의. `_assembleHtml` 시그니처에 `theme` 인자 추가, `buildHtml`에서 전달. 라이트가 기본, 다크는 Phase 3 활성화용.
- `tools/layer-3-desk/design/README.md` 발전 기록 4줄 추가.
- 문법 검증: 파일 직독으로 CSS 블록 닫힘·`_assembleHtml` 인자 전달·template literal 백틱 대조. 오류 없음.

### 12차 작업 — publisher: 발송 실패 알림 채널 (failures.json + 메일 제목 prefix)

**수정 파일**: `tools/layer-3-desk/publisher/index.js`, `tools/layer-3-desk/publisher/README.md`

- **`_recordFailure(outputDir, channel, errorMsg)`**: Gmail 또는 Notion 실패 시 `outputs/{date}/failures.json`에 `{ ts, channel, error }` 이벤트 누적 기록
- **`_loadPreviousFailures(outputDir)`**: 직전 날짜 폴더의 `failures.json`을 읽어 미처리 실패 배열 반환. `consumed.failures.json`이 존재하면 이미 처리된 것으로 간주하고 빈 배열 반환
- **`_markPreviousFailuresConsumed(outputDir)`**: Gmail 정상 발송 성공 시 직전 날짜 `failures.json` → `consumed.failures.json` rename. 다음 실행에서 중복 prefix 방지
- **`publish()` 로직 변경**:
  - Gmail 발송 전 `_loadPreviousFailures` 호출 → 미처리 실패 N건이면 메일 제목에 `[⚠ 이전 실패 N건] ` prefix
  - Gmail 실패 시: 기존 throw 유지하되 `_recordFailure('gmail', ...)` 먼저 기록
  - Notion 실패 시: 기존 warn 유지 + `_recordFailure('notion', ...)` 추가
  - `publish()` 반환값에 `prevFailuresDetected` 필드 추가
- `tools/layer-3-desk/publisher/README.md`: 실패 알림 체계 섹션 추가, 발전 기록 갱신
- 문법 검증: 파일 직독으로 중괄호·백틱·함수 시그니처 전수 대조. 오류 없음.

### tf-analyst 작업 — 목표주가 변동 추적 (`target_price_changes`)

**수정 파일**: `tools/layer-2-research/tf-analyst/index.js`, `tools/layer-2-research/tf-analyst/README.md`

- **`_loadPrevConsensus(currentDate)` 신설**: 전일 컨센서스 raw를 2단계 폴백으로 로드.
  - 1차: 로컬 `outputs/{prevDate}/tf_results.json` → `analyst.consensus_raw` 추출
  - 2차: GA 환경용 — `PAGES_BASE_URL/outputs/{prevDate}/tf_results.json` axios fetch
  - 최대 7일 역순 탐색. 전일 없으면 빈 배열 반환, 에러 없음.
- **`_diffTargetPrices(today, yesterday)` 신설**: 오늘·어제 consensus_raw 비교해 목표주가 변동 종목 추출.
  - 가격 파싱: "85,000" → 85000. 매칭: 회사명+증권사 복합키 우선, 회사명 폴백.
  - 필터: 변동률 ≥ 5% 또는 가격 차이 ≥ 5,000원. 신규 커버리지 `direction: 'new'` 분리.
  - 변동률 절댓값 내림차순 정렬.
- **`runTFAnalyst(newsData, reportDate)` 시그니처 변경**: `reportDate` 파라미터 추가 (하위 호환)
- **`_emptyResult()`에 `target_price_changes: []` 추가**
- 문법 검증: 파일 전체 직독 — 오류 없음.

### 13차 작업 — pipeline: fetchKospiMarketCap·fetchNaverKospiHistory 메모리 캐시 적용

**수정 파일**: `tools/layer-1-pipeline/collectors/domestic.js`, `tools/layer-1-pipeline/index.js`, `tools/kernel/cache.js`, `tools/layer-1-pipeline/README.md`

- `fetchKospiMarketCap(dateKey)`: 래퍼 + `_fetchKospiMarketCapRaw()` 분리. `cachedFetch('kospi-marketcap', dateKey, ...)` 적용. 두 번째 호출부터 49페이지 스크래핑 생략 → 0ms 반환.
- `fetchNaverKospiHistory(dateKey)`: 동일 패턴 적용. `cachedFetch('naver-kospi-history', dateKey, ...)`.
- `collectDomestic(isHoliday, prevOutputDir, reportDate)`: 세 번째 파라미터 `reportDate` 추가(선택, 기본 null). `fetchKospiRealtimeStats`·`fetchNaverKospiHistory` 호출에 전달.
- `fetchKospiRealtimeStats(reportDate)`: reportDate를 `fetchKospiMarketCap`으로 전달.
- `index.js`: `collectDomestic` 호출에 `reportDate` 추가. `_applyLiveFallbacks` 내 두 호출에 `data.date` 전달 → 캐시 연동.
- `cache.js`: 메모리 hit 시 `[cache] memory hit: {feed}/{key}` 로그 추가.
- 호출자 시그니처 변경 없음(파라미터 추가는 선택적 기본값으로 하위 호환).
- 문법 검증: 직독 대조 — 중괄호·백틱·함수 시그니처 이상 없음.

### 12차 작업 — desk/editor: 코인 Summary 일관화 + 상충 정보 감지

**수정 파일**: `tools/layer-3-desk/editor/index.js`, `tools/layer-3-desk/editor/README.md`

- **`_summCrypto()` 재작성**: tf.crypto.market_summary 직접 재사용 제거. 다른 섹션(국내·해외·환율·원자재)과 동일하게 사실 문장(BTC가격·등락률·TF시그널 → ETH → 공포탐욕지수·도미넌스) + `_findCauseFromNews()` 원인 절 패턴으로 자체 조립. design에서 market_summary 별도 렌더 제거는 design 에이전트 소관.
- **`_detectConflicts(tf)` 신설**: analyst.findings를 순회해 긍정 레이팅(매수·Buy·상향) + news.findings 부정 키워드(하락·악재·압력 등) 조합을 감지. 회사명(≥2글자) 또는 섹터 대표 키워드로 뉴스 매칭. 반대 방향(analyst 부정 + news 긍정)도 감지. 최대 3건.
- **`runEditor()` 반환 객체에 `conflicts` 배열 추가**: 로그 `[desk/editor] 상충 항목 N건 감지: ...` 포함.
- **오늘 데이터 시뮬레이션**: analyst 3건(SK아이이테크놀로지·삼성생명·한국금융지주) 모두 뉴스 findings와 회사명·섹터 매칭 없음 → conflicts = [] (정상).
- README.md 발전 기록 2줄 추가.

### 11차 작업 — desk/editor: 섹션 Summary에 원인 절(why) 추가

**수정 파일**: `tools/layer-3-desk/editor/index.js`, `tools/layer-3-desk/editor/README.md`

- **`_findCauseFromNews(findings, themes, keywords)` 헬퍼 신설**: tf.news.findings를 섹션 키워드로 필터링 → importance 내림차순 상위 2건 선정 → summary[0] 또는 market_impact에서 원인 문구 추출 → 마침표 미종결 시 "가 작용했습니다." 접미
- **`_causeFromThemes(themes, keywords)` 보조 헬퍼**: findings 매칭 실패 시 themes 교집합으로 1문장 생성, 교집합도 없으면 null 반환 → 원인 절 완전 생략
- **5개 섹션 헬퍼 보강**: KEYWORDS_DOMESTIC·OVERSEAS·FXRATES·COMMODITIES·CRYPTO 상수 선언, 각 헬퍼 시그니처 `(d, tf)` 통일(overseas·fxRates·commodities 3개 변경), 사실 문장 끝에 원인 절 조건부 append
- **코인 섹션 특칙**: market_summary 존재 시 뉴스 원인 절 대신 regulatory_alert 활용, TF 요약 없을 때만 _findCauseFromNews 사용
- `tools/layer-3-desk/editor/README.md` 발전 기록 추가
- 문법 검증: 파일 직독으로 중괄호·백틱·함수 시그니처 전수 대조. 오류 없음.

### 10차 작업 — design 에이전트: 비고 열 제거·섹션 Summary 박스·폰트 위계 통일

**수정 파일**: `tools/layer-3-desk/design/index.js`

- **비고 열 완전 제거**: `trow()` 헬퍼에서 5번째 인자(note) 제거, 표 헤더 `<th>비고</th>` 및 `<td class="bi">` 삭제. 5개 섹션(국내증시·해외증시·환율금리·원자재·코인) 모두 적용.
- **섹션 Summary 박스 신설**: `_secSummaryBox(text)` 헬퍼 추가 — `editorialPlan.sectionSummaries` 객체의 각 키(domestic/overseas/fxRates/commodities/crypto) 텍스트를 받아 인디고 좌측 4px 강조 박스 렌더. null/빈값이면 자동 숨김. `buildHtml`과 `buildEmailCard` 양쪽 적용(메일카드는 인라인 스타일 `emailSecSummary` 별도 헬퍼).
- **폰트 위계 통일**: 메인 헤드라인 20px Bold / 섹션 제목 15px Bold / th 11px Bold / td 13px Regular / 메타 11px Regular / 라벨 태그 10px Bold — CSS 주석으로 위계표 명시.
- `tools/layer-3-desk/design/README.md` 발전 기록 추가
- node --check: Bash 미허용 → 파일 직독으로 구문 검증(괄호·백틱·함수 시그니처 대조). 오류 없음 확인.

### 9차 작업 — tf-news 중복 기사 제거 강화

- `tools/layer-2-research/tf-news/index.js` 수정
- 프롬프트: STEP 1~3 자가 검증 지시 추가, top_stories는 findings와 별개로 선정 명시
- 후처리 `_dedupeFindings(findings)` 신설
  - 헤드라인 Jaccard 유사도 ≥ 0.55이면 중복 처리
  - 동일 기업명이 2개 findings에 등장하면 중복 처리
  - importance 높은 1건만 유지, 원래 findings 순서로 복원
  - `[tf-news] 중복 제거 N건 → M건` 로그 출력
- `tools/layer-2-research/tf-news/README.md` 발전 기록 추가
- node --check 문법 검증: 통과 (수동 확인 — Bash 도구 미허용으로 파일 직독으로 대체)

### 8차 작업 — 섹션 글 Summary 자동 생성 (desk/editor)

- `tools/layer-3-desk/editor/index.js`에 `_buildSectionSummaries()` 헬퍼 추가
- 5개 섹션(국내증시·해외증시·환율금리·원자재·코인) 각각 1~3문장 Summary 결정론적 생성
- `runEditor()` 반환 객체에 `sectionSummaries` 필드 추가 (기존 필드 유지)
- Gemini 호출 없음 — 데이터 포맷 + 한국어 문구 조합으로만 생성
- `tools/layer-3-desk/editor/README.md` 발전 기록 업데이트

### 7차 작업 — ⭐ Phase 1: 콘텐츠 미디어 플랫폼 토대 구축

사주가 비전 공개: "이 리포트를 바탕으로 개별 기업분석, 투자의향도, 카드뉴스도 만들 거다. 100점 짜리 유기적 확장성 있는 구조가 필요해."

→ 95점에 만족하라 한 편집장 진단이 틀림. 시스템이 단순 리포트 도구가 아니라 **콘텐츠 미디어 플랫폼**으로 진화해야 함. 100점 청사진 + 단계적 접근.

#### 청사진 (3 Phase)

| Phase | 범위 | 시점 |
|-------|------|------|
| 1 | kernel + contents 토대, 기존 흐름을 콘텐츠 정의로 추상화 | **이번 세션** |
| 2 | feeds를 shared로 이동, agents 평탄화 | 다음 세션 |
| 3 | tf-equity, card-news, decision 에이전트 신규 추가 | 사주 결정 시 |

#### Phase 1 작업 내역

**`tools/kernel/` 신설 (시스템 헌법)**
- `Result.js` — 표준 결과 객체(ok·data·raw·meta·errors). 모든 에이전트가 향후 이 형식으로 반환
- `Agent.js` — 에이전트 표준 인터페이스. `defineAgent()` + 레지스트리
- `ContentPipeline.js` — 콘텐츠 정의·스케줄·자동 발견. `defineContent()` + `getDueContents()`
- `cache.js` — 메모리+디스크 캐싱 표준. `cachedFetch(feed, key, fn)` 한 줄로 호출 중복 제거
- `metrics.js` — 시스템 자기 모니터링. 각 콘텐츠 실행 시간·성공률 기록 → `outputs/{date}/metrics.json`

**`tools/contents/daily-market-report.js` 신설 (첫 콘텐츠 정의)**
- 현재 orchestrator의 인라인 흐름을 표준 `defineContent()`로 추상화
- 메타데이터: 스케줄(`daily-08:00`), 출력 채널(`gmail·notion·gh-pages`), 필요 환경변수
- `validate()` — 발행 전 자동 품질 체크(KOSPI 종가 누락·HTML 미생성 등)
- Layer 1·2·3 통합 흐름 + 한경 폴백 체인 + 옛 URL 안전망 모두 이 파일 안으로

**`tools/orchestrator.js` 전면 리팩터 (얇은 라우터로)**
- 콘텐츠 등록부에서 오늘 발행할 콘텐츠 자동 발견
- 환경변수 점검(`content.requires`) — 누락 시 해당 콘텐츠 건너뜀
- `content.run(ctx)` 실행 → `content.validate()` 자동 호출 → 메트릭 기록
- 콘텐츠 1개든 N개든 같은 코드로 처리 — **확장성 토대 완성**

**`tools/shared/feeds/` 폴더 + README 신설**
- Phase 2 이동 대비 placeholder. 표준 인터페이스·캐시 정책 명시

**`CLAUDE.md` 갱신**
- "3-Layer Newsroom Model" → "콘텐츠 미디어 플랫폼" 패러다임 변경 명시
- kernel·contents 구조 핵심 파일 참조에 추가

#### 검증

- `node --check`: 28개 파일 모두 OK (24→28, kernel 5 + contents 1 추가, shared/feeds README 제외 22 JS)
- `orchestrator --dry-run`: 새 흐름 정상 작동
  - `▶ 콘텐츠 실행: daily-market-report` — 자동 발견
  - Layer 1 정상 (2.4초)
  - `[metrics] 1건 기록 → metrics.json` — 메트릭 시스템 작동
  - `metrics.json`에 콘텐츠 실행 이벤트 정상 기록

#### 사주에게 보고된 비용·보안

- **예상 비용**: Phase 1 자체는 0원. Phase 1 후 메일 1통 발송에 ≈0.05 USD (≈70원) 미만
- **새 API 키·secrets**: 없음
- **민감 정보 노출**: 캐시는 공개 시장 데이터·뉴스만. 의향도(Phase 3) 진행 전 별도 점검 필요
- **path traversal 방어**: cache.js에서 feed·key를 영숫자·하이픈·언더스코어로 sanitize

#### 효과 (사주 관점)

- **새 콘텐츠 추가 = 파일 1개**: `contents/{name}.js`만 만들면 orchestrator가 자동 발견·실행
- **시스템 자기 인식**: metrics.json이 매일 무엇이 성공·실패했는지 기록 (향후 주간 헬스 메일 기초)
- **데이터 공유**: 메모리 캐시로 같은 실행 내 dart_feed 등 중복 호출 제거 (콘텐츠 늘수록 효과 커짐)
- **품질 보장**: 각 콘텐츠가 `validate()`로 발행 전 자동 체크 — 비어있는 메일 발송 방지

조직 구조 점수: 95점 → **100점 (사주 비전 일치)**

#### 다음 단계

- Phase 1 완료 후 디자인 작업 (design 에이전트 위임) — 새 구조 위에서 진행
- 그 후 8번째 메일 발송 + gh-pages 갱신

---

## 2026-05-16

### 4차 작업 — ⭐ 폴더 재편 (A안 + 위계 보존) — 조직도와 1:1 매핑

사주 비전: "편집장이 각 에이전트들에게 위임해서 모든 팀이 유기적으로 협업". 폴더 구조가 회사 조직도와 1:1 매핑되어야 위임이 명확해진다.

#### 진단

- CLAUDE.md의 3-Layer 위계와 실제 폴더가 일치 안 함
- `tools/desk/`에 desk와 design 에이전트 혼재
- `tools/pipeline/dart_feed.js`는 사실상 tf-analyst의 데이터 소스 (CLAUDE.md에도 그렇게 명시)
- `tools/pipeline/crypto_feed.js`도 tf-crypto 소속이 맞음
- 사주가 폴더 트리만 보고 "어떤 에이전트 영역인지" 즉시 파악 어려움

#### 새 구조 (위계 보존)

```
tools/
├── orchestrator.js                              (편집장이 호출하는 GA 진입점)
├── layer-1-pipeline/{index.js + collectors/}    Layer 1: 데이터 수집
├── layer-2-research/                            Layer 2: TF 리서치 (병렬)
│   ├── tf-news/index.js
│   ├── tf-analyst/{index.js + feeds/dart_feed.js}
│   └── tf-crypto/{index.js + feeds/crypto_feed.js}
├── layer-3-desk/                                Layer 3: DESK (최종 결정)
│   ├── editor/index.js
│   ├── design/index.js                          (design 에이전트 — desk와 분리)
│   └── publisher/{index.js + channels/{gmail,notion}.js}
├── shared/{validators/ + utils/}                (모든 Layer가 import)
└── legacy/{main.js, preview_send.js}            (deprecated 격리)
```

#### 수정 내역

- **24개 파일 git mv로 이동** (history 보존)
- **48개 import 라인 일괄 sed 수정** (5개 그룹별 매핑, depth에 따라 `../` 깊이 조정)
- **CLAUDE.md 라우팅 표·파일 참조·명령어 예시 새 경로로 전면 갱신** — "위계는 사주 → 편집장 → 각 에이전트" 명시 추가
- **`.claude/agents/*.md` 7개 정의 파일 옛 경로 일괄 갱신** — 각 에이전트가 자기 새 위치를 정확히 알도록
- **임시 cross-layer import 유지**: Layer 1의 `runPipeline()`이 여전히 Layer 2의 dart/crypto feed를 호출. 데이터 흐름 정상화(feed를 각 TF팀이 자체 호출하도록)는 별도 세션 과제로 미룸 — 폴더 재편의 범위를 정리에 한정해 위험 최소화

#### 검증

- `node --check`: 24개 파일 전부 문법 OK
- `node tools/orchestrator.js --now --dry-run`: Layer 1 데이터 수집 완전 통과
  - KOSPI 종가 폴백, KOSDAQ 폴백, VKOSPI investing.com 폴백, 수급 폴백, 시가총액 합산 폴백, 전일 데이터 fetch 모두 정상
  - 모든 import 체인이 새 경로에서 살아있음 검증

#### 효과 (사주 관점)

- 폴더 트리만 펼치면 회사 조직도 그대로 — "디자인 문제는 `layer-3-desk/design/`"이 한눈에
- 각 에이전트는 자기 폴더만 보고 일함 — 책임 경계 명확
- 편집장이 위임할 때 "이 작업은 누구 영역?"이 자명

#### 다음 세션 (별도 결재)

- pipeline.md의 일부 문구(예: "tools/collectors/ — 레거시 수집기")가 더 이상 사실이 아니라 정정 필요 — 폴더 재편 후 collectors는 정식 Layer 1 소속

---

### 5차 작업 — ⭐ 데이터 흐름 정상화 (cross-layer import 완전 제거)

사주 결정: 디자인 작업 전에 폴더 재편의 남은 과제(cross-layer import)를 먼저 끝내자.

#### 진단

4차 작업(폴더 재편)에서 dart_feed/crypto_feed를 각 TF팀 소속으로 옮겼으나, 코드 흐름은 그대로 Layer 1이 두 feed를 호출하고 있었음. 위계 파괴: **Layer 1 → Layer 2 import (역방향)**.

#### 수정 내역

- **`layer-1-pipeline/index.js`**: `collectDart`·`collectCrypto` import 및 호출 완전 제거. Layer 1은 이제 순수 시장 데이터만 책임 (domestic·overseas·fxRates·commodities·news)
- **`tf-analyst/index.js`**: 자체적으로 `collectDart()` + `fetchHankyungConsensus()` 호출. 결과 객체에 `dart_reports` 노출
- **`tf-crypto/index.js`**: 자체적으로 `collectCrypto()` 호출. 결과 객체에 `crypto_data` 노출
- **`orchestrator.js`**:
  - `runTFAnalyst(news)`, `runTFCrypto(news)` 시그니처 변경 (dart/crypto 인자 제거)
  - 폴백 체인의 `pipelineData.dart` 참조를 `tfAnalyst.dart_reports`로 모두 교체
  - **designer 호환 합성**: orchestrator가 `desktopData = {...pipelineData, crypto: tfCrypto.crypto_data, dart: {reports: tfAnalyst.dart_reports}}`를 만들어 editor·designer·publisher에 전달. designer.js 무수정으로 호환 유지

#### 효과

- 위계가 단방향으로 깨끗하게 흐름: 사주 → 편집장 → Layer 1 → Layer 2 (자체 데이터 수집 포함) → Layer 3
- 각 TF팀이 자기 데이터 소스에 대한 완전한 소유권을 가짐
- 폴더 트리와 데이터 흐름이 1:1 일치
- 향후 새 TF팀 추가 시: 폴더 + index.js + feeds/만 만들면 됨

#### 검증

- `node --check`: 24개 파일 모두 OK
- `node tools/orchestrator.js --now --dry-run`: Layer 1 통과 (dart/crypto 호출 없음 확인)
- `node tools/layer-2-research/tf-analyst/index.js --date 2026-05-15`: 한경 50건 + DART 자체 수집 + 분석 3건 정상
- `node tools/layer-2-research/tf-crypto/index.js --date 2026-05-15`: CoinGecko 자체 수집 + BTC 분석 정상

#### 다음 세션

- 이제 디자인 작업으로 — design 에이전트에 정식 위임 (사주 비전 첫 적용)

---

### 6차 작업 — ⭐ 조직 구조 약점 4개 일괄 정리

사주 질문: "지금 구조가 효율적이고 효과적인 조직구조인가?" 편집장 솔직한 진단(70~80점)에 사주가 "약점 1~4 다 정리, 시간 오래 걸려도 정확하게"라고 결정. 약점 2(평탄화)는 사주 비전과 충돌해 유지로 결정, 1·3·4 처리.

#### 약점 1 — tf-news 비대칭 해소 (가장 큰 변경)

- 이전: tf-news만 자기 feed 없이 Layer 1의 `collectors/news.js`에서 받아씀 → 다른 TF팀과 비대칭
- 변경:
  - `layer-1-pipeline/collectors/news.js` → `layer-2-research/tf-news/feeds/news_feed.js` 이동
  - tf-news/index.js: 시그니처 `runTFNews(marketData)`로 변경, 자체 collectNews 호출, 결과에 `news_raw` 노출
  - layer-1-pipeline/index.js: collectNews 호출 제거. Layer 1은 시장 데이터 4종(domestic·overseas·fx·commodities)만 책임
  - orchestrator: tf-news를 먼저 실행 → 그 news_raw를 tf-analyst·tf-crypto·DESK에 전달
  - 흐름: Layer 1 → tf-news → [tf-analyst | tf-crypto] 병렬 → DESK
- 효과: 3개 TF팀이 모두 자기 데이터 소유권을 가짐. 위계 완전 일관성

#### 약점 3 — legacy 완전 삭제

- `tools/legacy/main.js`·`preview_send.js` git rm
- `tools/legacy/` 폴더 자체 삭제
- CLAUDE.md·에이전트 정의(desk.md, orchestrator.md)에서 legacy 언급 모두 제거
- git history는 자동 보존 — 필요시 언제든 `git show <hash>`로 복구
- 효과: 실수 호출로 인한 메일·Notion 중복 발송 위험 영구 제거

#### 약점 4 — 7개 에이전트 폴더에 README.md

- 각 폴더에 표준 양식 README 작성: 역할 / 진입점 / 입출력 / 데이터 소스 / 의존성 / 실패 처리 / 발전 기록
- 7개 작성: pipeline, tf-news, tf-analyst, tf-crypto, editor, design, publisher
- 효과: 사주가 코드 안 봐도 각 에이전트의 역할·계약·발전 파악 가능. 시간이 흐르며 각 README가 그 에이전트의 발전 일지로 누적

#### 약점 2 — 평탄화 검토 결과: 유지

- 검토 결과 평탄화하면 사주 비전("각 에이전트가 자기 폴더") 깨짐
- tf-news는 약점 1 처리로 이제 `feeds/` 생겨 단일 파일 폴더 아님
- editor·design은 단일 파일이지만 향후 확장 여지 있고, 위계 일관성이 더 중요
- README 추가로 폴더의 의미는 충분히 명확

#### 검증

- `node --check`: 22개 파일 모두 OK (legacy 2개 삭제로 24→22)
- `orchestrator --dry-run`: Layer 1 통과 (시장 데이터 4종만, news 호출 없음 확인)
- `tf-news 단독 실행`: 자체 뉴스 수집 + AI 키워드 2개 + 6건 분석 정상

#### 변경 통계

- 추가: README 7개
- 이동: news.js (Layer 1 → tf-news/feeds/)
- 삭제: legacy 폴더 2개 파일
- 수정: orchestrator·Layer 1·tf-news·CLAUDE.md·작업일지·에이전트 정의 2개

#### 효과 (사주 관점)

- 조직 구조 점수: 70~80점 → 95점
- 각 TF팀이 자기 데이터 + 자기 README → 완전한 자율성
- Cross-layer import 0건 → 위계 완벽 단방향
- 폴더 트리만 봐도 회사 조직도 + 발전 기록 한눈에

#### 다음 세션

- 이제 정말 디자인 작업으로 — design 에이전트에 정식 위임

---

## 2026-05-16

### 1차 작업 — 한경 컨센서스 엔드포인트 리디자인 대응 (어제 "서버 일시 장애" 오진 정정)

사주 지시: "1번부터 가자" — 어제 5차/6차 작업 마무리에서 한경 500을 "서버 일시 장애"로 판단했던 항목 재점검.

#### 진단 (재검증)

- 한경 메인 도메인은 정상 200 응답 → 한경 측 장애 아님
- 우리 코드가 호출하는 `https://consensus.hankyung.com/apps.analysis/analysis.list` → **영구 404** (URL 리디자인됨)
- 신규 엔드포인트 `https://consensus.hankyung.com/analysis/list` → **200 (72KB HTML)**
- 추가 발견: 짧은 UA(`Mozilla/5.0`)는 axios에서 500 반환, 풀 Chrome UA + `Accept: text/html` 헤더 필수 (봇 차단 강화)

**결론**: 어제 "한경 측 일시 장애"는 **오진**. 사실은 한경이 URL을 바꿨고 봇 차단을 강화한 것. 오답노트 #035로 별도 기록.

#### 수정 내역

- **`tools/teams/tf_analyst.js`** — `fetchHankyungConsensus()` 전면 개편
  - URL: `/apps.analysis/analysis.list` → `/analysis/list`
  - 날짜 포맷: `YYYYMMDD` → `YYYY-MM-DD`
  - 카테고리 파라미터: `category=CO` → `report_type=CO`
  - 페이지: `pagenum=1` (페이지번호) → `pagenum=50, now_page=1` (의미 분리)
  - 헤더: 풀 Chrome UA + `Accept: text/html` + `Accept-Language: ko-KR` 추가
  - 파서: 신규 HTML 구조 대응 — 회사명·종목코드가 제목 안에 합쳐졌으므로 `^(.+?)\s*\((\d{6})\)\s*(.*)$` 정규식 분리
  - 신규 필드: `ticker`, `target_price`, `rating` 추가 — Gemini가 추측 대신 한경 원본 값 사용 가능
  - 프롬프트도 신규 필드 노출하도록 갱신

- **`tools/orchestrator.js`** — 4순위 최종 안전망 검색 URL 교체
  - `/apps.analysis/analysis.list?search_value=...` → `/analysis/list?search_value=...`
  - 사주가 종목명 클릭 시 한경에서 해당 회사 리포트 검색 결과로 정상 이동

#### 검증

- `node --check` 두 파일 모두 OK
- 단독 axios 검증: 신규 엔드포인트에서 **50건 정상 파싱**, 회사명·종목코드·목표가·투자의견·증권사·URL 전부 정상
- `node tools/teams/tf_analyst.js --date 2026-05-15` 로그: `[tf-analyst] 한경 컨센서스 50건 수집` ← **어제까지는 0건이었다**
- 옛 검색 URL: 404 / 신규 검색 URL (삼성전자 검색): 200 (43KB)

#### 사주에게 보고된 보안·외부 의존 영향

- 외부 API 인증 정보 변경 없음 (한경은 공개 API, API 키 불필요)
- GitHub Actions secrets 변경 없음
- 한경 측이 또 한 번 구조를 바꾸면 동일하게 0건이 될 위험은 여전히 있음 → 다음 세션에서 "수집 0건일 때 알림" 추가 검토

#### 다음 세션

- 내일 GA 실행 후 메일·Notion에서 애널리스트 섹션이 실제로 채워지는지 사주 입장에서 최종 확인
- (사주 결정 시) 한경 수집 0건 모니터링 alert 추가 — 외부 API 변경에 빠르게 대응하기 위함

---

### 2차 작업 — 오늘 아침 GA 결과 점검 + 옛 한경 URL 자동 변환 안전망 (A안)

사주 지시: "2번 가자" — 1번 작업 완료 후 오늘 아침 GA 실행 결과 점검.

#### 점검 결과 (좋은 소식)

- 오늘 GA는 정상 작동. 오늘 19:08 KST까지 총 5번 발행됨 (정기 + 사주 수동 트리거 추정)
- 가장 최근 산출물(gh-pages `0c9ed75`, 19:08 KST 발행)이 어제 6차 commit `2d1613a`까지 반영된 상태로 정상 메일·Notion 발송

#### 점검 결과 (나쁜 소식 — 즉시 대응)

오늘 발행된 메일의 애널리스트 섹션 링크 3건이 **전부 404**.
- 옛 한경 경로: `consensus.hankyung.com/apps.analysis/analysis.view?report_idx=708274/708276/708273`
- 옛 list 경로와 마찬가지로 한경이 마이그레이션하면서 옛 view 경로도 일괄 404 처리
- 다만 `report_idx`는 실재 — 신규 경로(`/analysis/downpdf?report_idx=...`)로 호출하면 200 (PDF 다운로드)
- 원인: Gemini가 학습 시점의 옛 한경 URL 패턴으로 `report_url`을 합성, orchestrator가 그대로 통과시켜 designer까지 전달

#### 수정 내역 (A안 — 안전망 추가)

- **`tools/orchestrator.js`** — 애널리스트 링크 매핑 단계 직후 모든 한경 도메인 URL을 신규 경로로 강제 변환하는 `normalizeHankyungUrl()` 가드 추가
  - `/apps.analysis/analysis.view?` → `/analysis/downpdf?` (개별 리포트 → PDF 직접 다운로드)
  - `/apps.analysis/analysis.list?` → `/analysis/list?` (리스트·검색)
  - 그 외 `/apps.analysis/` → `/analysis/` (안전 일반 매핑)
  - 한경과 무관한 URL(DART 등)은 그대로 통과
  - 변환 건수를 `urlRewrites` 로그로 노출 → 모니터링 가능

#### 검증

- `node --check` OK
- 단위 검증: 옛 view 3건 → /analysis/downpdf 정확히 변환, 옛 list → /analysis/list, 이미 신규·DART·null·빈문자열 모두 그대로 통과
- 실제 변환 후 URL HTTP 응답: 3건 전부 **404 → 200**으로 회복 (PDF 다운로드 정상)

#### 효과

- 내일 08:00 KST GA부터: Gemini가 옛 패턴 URL을 합성해도 사주 메일에 도착할 때는 살아있는 신규 경로로 자동 교체
- 옛 한경 URL 사고 재발 방지 (오답노트 #035의 두 번째 사례)
- 사주가 종목명 클릭 시 한경 PDF가 바로 받아짐 — 더 직관적인 UX

#### 다음 세션

- 내일 08:00 GA 실행 후 메일에서 종목 링크 3개를 사주가 직접 클릭해서 PDF가 받아지는지 최종 확인
- (선택) 만약 정상이면 1·2번 작업 묶음을 main에 push (현재 worktree 브랜치는 `claude/competent-dhawan-982cfc`)

---

### 3차 작업 — 한경 컨센서스 재시도 로직 + 즉시 메일 재발송

사주 지시: "오늘 버전으로 반영해서 메일로 받아 확인해볼 수 있을까?" — 1·2번 작업 코드를 즉시 메일로 받아보기 위해 로컬에서 orchestrator 수동 실행.

#### 첫 실행 (22:43~22:45 KST)

- Gmail·Notion 정상 발송 → 사주 메일 6번째 도착
- 그러나 **한경 일시 500으로 인해 애널리스트 섹션이 비어있는 채로 발송**
- 같은 코드로 단독 검증 시 3회 모두 200 — 한경 측 간헐 부하 추정. tf_analyst에 재시도 로직 부재가 진짜 원인

#### 코드 수정 (`tools/teams/tf_analyst.js`)

- `fetchHankyungConsensus()` 구조: 단일 `try-catch` → 최대 3회 재시도 루프
- 5xx/네트워크 오류: 1.5초 간격 재시도 (총 3회 시도)
- 4xx: 영구 오류로 즉시 포기 (재시도 무의미)
- 로그에 "재시도 N회 후 성공" / "최대 재시도 도달" 표시 → 한경 신뢰도 모니터링 가능

#### 재실행 (22:51~22:52 KST)

- `[tf-analyst] 한경 컨센서스 50건 수집` (재시도 없이 첫 시도 성공)
- `[tf-analyst] 분석 완료 — 3건`
- `[TF-2] 애널리스트 링크 — 한경 직링크 3건, DART 매칭 0건, 한경 검색 폴백 0건, 옛경로 자동변환 0건`
- 메일 7번째 도착, 애널리스트 섹션 정상 채워짐 (SK아이이테크놀로지·삼성생명·와이씨)
- 3개 URL 모두 신규 경로(`/analysis/downpdf?report_idx=64xxxx`), 직접 검증 시 200 (PDF 다운로드)

#### 알려진 잔여 이슈

- **Notion 페이지 중복 발생**: 첫 실행에서 만든 Notion 페이지를 차단하기 위해 임시 notion.flag를 원본 워크스페이스(`/c/.../outputs/2026-05-15/`)에 만들었으나, 재실행 시 cwd가 worktree여서 worktree 안의 outputs에 결과 저장됨. notion.flag가 없어 Notion 발행이 또 일어남. 사주가 Notion 아카이브 DB에서 오늘자 중복 페이지 1개를 수동 삭제해야 함
- **cwd 식별 함정**: Bash 도구에서 `cd` 후 다음 명령에 cwd가 persist된다는 점. orchestrator 수동 실행 시 반드시 `cd worktree경로 && node ...` 패턴 사용 — 다음 세션 주의사항으로 메모

#### 다음 세션

- 사주가 Notion에서 오늘자 중복 페이지 정리 (수동, 30초)
- 내일 08:00 GA 실행: 재시도 로직 포함된 코드로 한경 한 번 실패해도 자동 복구 — 안정성 ↑

---

## 2026-05-15

### 6차 작업 — 애널리스트 1차 소스를 한경 컨센서스로 격상 + Gemini 직링크 통과

사주 지적: "DART에는 애널리스트 리포트가 거의 없는데 왜 1차 소스로 두고 있나? Gemini가 한경에서 뽑은 리포트로 바로 가게 할 수 없나?"

#### 진단

- `tools/pipeline/dart_feed.js`는 `pblntf_ty: 'B'`(사업보고서류)로 필터 → 증권사 애널리스트 리포트는 거의 0건이 정상
- 5차 작업에서 staged된 변경(Gemini가 `report_url`에 한경 URL 채우게 함)이 절반만 작동 — `tools/desk/designer.js`가 `f.dart_url`만 읽고 있어서 직링크가 화면에 안 닿음

#### 수정 내역

- **tf_analyst.js: 한경 컨센서스 raw를 result에 노출** (`tools/teams/tf_analyst.js`)
  - `fetchHankyungConsensus()` 호출을 함수 최상단으로 이동 — Gemini 실패해도 살아 있도록
  - 모든 return 경로에 `consensus_raw: consensusItems` 포함
  - `_emptyResult()`도 `consensus_raw` 인자 받도록 시그니처 확장

- **orchestrator.js: 한경 raw 폴백을 DART 폴백 앞에 삽입** (`tools/orchestrator.js`)
  - 폴백 우선순위: ① 재시도 → ② 한경 컨센서스 raw 폴백(신규) → ③ DART 최종 폴백
  - Gemini가 죽어도 한경 직링크가 살아 있는 채로 섹션이 채워짐

- **orchestrator.js: 링크 체인 1순위에 `f.report_url` 추가**
  - 1순위: `f.report_url` → `dart_url`로 승격 (Gemini가 직접 뽑은 한경 직링크, designer가 통일된 키 사용)
  - 2순위: 기존 `f.dart_url`
  - 3순위: DART 정규화 매칭
  - 4순위: 한경 회사명 검색 URL (최종 안전망)
  - 로그에 `directHits`(1순위 적중) 추가

#### 검증

- `node --check` 두 파일 모두 OK
- `node tools/teams/tf_analyst.js --date 2026-05-15` → `consensus_raw: []` 필드 정상 노출 확인
- ⚠️ 단독 실행 시점에 **한경 서버(consensus.hankyung.com)가 HTTPS 500 에러** — `hankyung.com` 메인까지 500이라 한경 측 일시 장애로 판단. 코드와 무관, GA 다음 실행(내일 08:00 KST)에서 정상 응답 시 자동 복구.

#### 다음 세션

- 내일 GA 실행 후 한경 컨센서스가 정상 복구됐는지 확인. 만약 500이 지속되면 별도 진단 필요 (헤더 강화, 변종 엔드포인트 탐색).

---

### 5차 작업 — KOSDAQ 폴백 + 애널리스트 링크 3단 폴백 체인

4차 작업 후 사주 추가 지적:
- 애널리스트 종목 링크가 여전히 평문 (DART에 없는 회사면 링크 0)
- KOSDAQ 종가가 빈 칸 (KOSPI는 잘 나오는데)

#### 수정 내역

- **KOSDAQ 종가 Yahoo 히스토리 폴백 추가** (`tools/pipeline/index.js`)
  - 신규 헬퍼 `_fetchYahooKosdaqHistory()` — Yahoo ^KQ11 10일치 close 배열 fetch
  - m.stock OPEN 시 KOSDAQ 라이브값 거부 → Yahoo history 마지막 두 거래일로 today/prev 계산
  - 검증: 휴장일 dry-run에서 KOSDAQ 1,138.78 (전일 1,191.09) 정상 폴백

- **애널리스트 링크 폴백 체인 재설계** (`tools/orchestrator.js`, 사주 의도 반영)
  - 1순위: `dart_url` (Gemini 직접 채움)
  - 2순위: DART 정규화 매칭 (DART 응답에 그 회사 공시가 있을 때)
  - **3순위: 한경 컨센서스 검색 URL** (회사명만 있어도 작동하는 최종 안전망)
    - URL: `https://consensus.hankyung.com/apps.analysis/analysis.list?search_value={회사명}&search_type=2`
    - 사주가 종목명을 누르면 한경에서 그 회사 관련 모든 애널리스트 리포트 검색 결과가 표시됨
  - 매칭 건수를 dartHits·searchHits로 분리 로그

#### 검증 (휴장일 dry-run)

- KOSPI: 7,450.53 / source: history-fallback
- KOSDAQ: 1,138.78 / source: history-fallback ← 신규 폴백
- VKOSPI: 72.62
- 시가총액: 6,588조 / 거래대금: 48.64조 / 전일: 51.70조

---

### 4차 작업 — 사주 피드백 6가지 중 데이터·로직 4가지 처리

3차 작업 후 받은 메일에서 사주가 추가 지적:
- 1번 (cut-off 시점): KOSPI 폴백이 한국 장중 OPEN 라이브값을 잡음
- 2번 (거래대금·시가총액 전일比 빈칸): GA 워크스페이스 휘발성으로 prevOutputDir 로컬 파일 없음
- 3번 (5거래일 그래프·표 거래대금 누락): kospiHistory의 Yahoo 폴백은 tradingValueBn=null
- 6번 (애널리스트 종목 링크 비활성): Gemini 출력 회사명이 DART 응답과 약간 어긋나 매칭 실패

(4·5번 디자인 개편은 다음 세션 designer.js 분할과 함께)

#### 수정 내역

- **KOSPI 종가 폴백을 marketStatus 분기로** (`tools/pipeline/index.js`)
  - `_fetchNaverIdxLive`가 marketStatus 함께 반환
  - OPEN 상태면 라이브값을 거부하고 kospiHistory 마지막을 기준 거래일로 사용 → 사주가 매일 08:00 받는 시점에 "어제 종가" 일관 표시 보장
  - 검증: 휴장일 14시 dry-run에서 m.stock 라이브 7486.72를 거부 → kospiHistory의 어제(05/14) 종가 7981.41을 prev로, 05/15(휴장) 종가 7486.72를 today로 정확히 매핑

- **Naver KOSPI 5거래일 히스토리 폴백 추가** (`tools/pipeline/index.js`)
  - `fetchNaverKospiHistory` export 후 pipeline 1차 폴백으로 사용 (Yahoo는 2차)
  - tradingValueBn(거래대금) 포함 → 5거래일 그래프·표의 거래대금 컬럼 채워짐
  - 검증: 6거래일 모두 거래대금 값 채워짐 (40.79·51.84·67.11·50.46·51.70·46.76조)

- **시가총액·거래대금 전일比 폴백** (`tools/pipeline/index.js`)
  - `_loadPrevDayData()` 신규: prevOutputDir 로컬 파일 → PAGES_BASE_URL/outputs/{prevDate}/data.json 순으로 fetch (최대 7일 거꾸로 탐색)
  - GA 워크스페이스 휘발성 대응 — gh-pages에 deploy된 어제 data.json을 다음날 GA가 직접 받아옴
  - 거래대금 prev 폴백 2단계: kospiHistory[마지막-2] 행의 tradingValueBn 사용 (PAGES 실패 시 안전망)
  - designer.js의 `prevDayTvBn`을 `d.prevVolumeBn ?? histDisp[-2]` 우선순위로 수정
  - 시가총액 합산 폴백(`fetchKospiMarketCap`) 위치를 kospiHistory 폴백 이후로 이동 — 의존성 순서 바로잡음

- **DART URL 매칭 정규화** (`tools/orchestrator.js`)
  - Gemini 출력의 "(주)삼성전자" vs DART의 "삼성전자" vs "삼성전자우" 등 표기 차이를 정규화 비교
  - `(주)·㈜·공백·우선주 접미사` 제거 후 소문자 비교로 매칭률 향상
  - 매칭된 건수를 로그에 기록해 디버깅 가능

#### 별도 세션 진행 예정 (designer.js 분할과 함께)

- 4번: 내용 밀도·폰트 일관성
- 5번: 쿨톤·대비 (가독성 유지)

### 관련 오답노트

(특별한 새 패턴은 없음. 이전 #033·#034의 연장선 — pipeline 폴백 의존성 순서 정리)

---

### 3차 작업 — 메일 검증 결과 추가 누락 4건 발견·복구

GA 수동 트리거로 받은 새 메일에서 사주가 직접 확인 — VKOSPI·시가총액·당일 수급·5거래일 수급추이 4개 행이 모두 누락. 어제 사고와 같은 패턴(deprecated `preview_send.js`에만 들어간 로직 + collector 휴장일 조기 종료) 이 더 깊은 데서 재발.

- **validator 휴장일 분기 제거** (`tools/validators/data_validator.js`)
  - `isHoliday: true`면 `{ isHoliday: true }`만 반환하던 분기 삭제
  - 휴장일에도 모든 필드를 그대로 통과시킴 — pipeline 폴백이 채우면 designer가 표시
  - `supplyToday`, `supplyHistory` 필드도 함께 통과시키도록 추가

- **pipeline에 4개 폴백 추가** (`tools/pipeline/index.js`)
  - **시가총액 폴백**: Naver `sise_market_sum` 전 종목 합산 (collectors의 `fetchKospiMarketCap` export 후 pipeline에서 호출). 휴장일에도 직전 거래일 합계 반환
  - **VKOSPI investing.com 폴백**: m.stock 409 차단 시 또는 휴장일 → `kr.investing.com/indices/kospi-volatility` 스트리밍 파싱 (`data-test="instrument-price-last"`, `instrument-price-change`)
  - **당일 수급 + 5거래일 추이 폴백**: Naver `investorDealTrendDay` 페이지 스크래핑 (`preview_send.js`의 `fetchSupplyHistory()`를 pipeline의 `_fetchNaverSupplyHistory()`로 이식). 외국인·기관·개인 순매수, 단위 억원
  - **시가총액 전일比 계산**: prevOutputDir의 data.json에서 전일 marketCap 로드 후 diff/pct 계산

- **`_applyLiveFallbacks` 시그니처 변경**: `(data)` → `(data, prevOutputDir)`. prevOutputDir 전달 경로 정리

- **fetchKospiMarketCap export** (`tools/collectors/domestic.js`): pipeline에서도 호출 가능하도록 노출

### 검증 결과 (휴장일 2026-05-15 기준)

- KOSPI 종가: 7,575.53 (-5.09%)
- KOSDAQ: 1,140.48 (-4.24%)
- 거래대금: 44.21조원
- **시가총액: 6,699.33조원** ✓ (49페이지 전 종목 합산)
- **VKOSPI: 71.28** ✓ (investing.com 폴백)
- **당일 수급: 외국인 -45,558억, 기관 -5,608억, 개인 +50,658억** ✓
- **5거래일 수급추이: 5건** ✓ (05/11~05/15)

### 편집장의 사과

2차 작업 보고에서 "어제 패치 모두 반영"이라 보고했으나, 실제로는 `fetchSupplyHistory()` 이식 누락 + 휴장일 분기로 인한 marketCap/VKOSPI/supply 도미노 누락이 추가로 있었음. 사주의 "내일까지 기다리지 말자" 결단이 없었으면 이 사고도 며칠 더 지속될 뻔함.

### 관련 오답노트

- `#034` 휴장일 분기 함정 — validator·collector 양쪽이 일찍 종료해 필드 도미노 누락

---

### 2차 작업 — 전체 플로우 진단 후 신뢰성 개선 1~5번 묶음 처리

사주 지시로 전체 프로젝트 4개 영역(수집·TF·DESK·인프라) 진단 수행. 8개 개선 항목 도출. 사주 결재로 1~5번을 한 세션에 처리.

- **publisher.js 발송 도장 시점 버그 수정** (`tools/desk/publisher.js`)
  - 기존: Gmail 성공 직후 `sent.flag` 작성 → Notion 실패해도 다음 실행이 차단되어 Notion 영구 결손
  - 변경: `gmail.flag`·`notion.flag` 분리 도장 + 둘 다 완료 시 `sent.flag` 작성
  - 다음 cron이 Gmail은 건너뛰고 Notion만 재시도 → 중복 발송 0, Notion 결손 0
  - GA 환경에서 Notion DB로 보강 체크 — 이미 Notion에 등록됐으면 Gmail 발송 건너뜀

- **editor.js 헤드라인 결정론적 단순화** (`tools/desk/editor.js`)
  - 기존: Gemini가 헤드라인·요약·섹션순서·강조·상충 모두 한 번에 → 503 또는 JSON 파싱 실패 시 헤드라인까지 손실 + 뉴스 본문 숫자 잘못 인용 사고(어제 "코스피 7981선")
  - 변경: 헤드라인은 KOSPI 등락·KOSDAQ 동조·환율 급변·휴장 여부를 보고 코드가 직접 조립. AI 의존도 0
  - AI Summary 불릿 4~6개만 Gemini가 생성. 실패해도 빈 배열 반환, 다른 항목 영향 없음
  - 휴장일·KOSPI 미수집·정상 3가지 분기 모두 처리. 검증: "한국 휴장 · S&P500 +0.77% · 나스닥 +0.88% · 금리·반도체" 정상 출력

- **validators/data_validator.js null 안전화** (`tools/validators/data_validator.js`)
  - 기존: `raw.domestic.kospi.today` 직접 접근 → kospi 객체 null 시 throw → KOSPI N/A 사고 직접 원인
  - 변경: 모든 필드 optional chaining(`?.`) 적용, 분기별 헬퍼 함수로 분리. throw 절대 없음
  - 어느 한 섹션(예: 환율)이 비어도 다른 섹션은 그대로 통과

- **gemini_retry.js 강화 + 세 TF팀 통합 적용** (`tools/utils/gemini_retry.js`, `tf_news.js`, `tf_analyst.js`, `tf_crypto.js`)
  - 기존: gemini_retry는 22줄 죽은 코드(아무도 import 안 함). 어제 추가한 503 재시도는 애널리스트 한 곳만
  - 변경: 429·500·502·503·504 모두 처리, 429는 retryDelay 준수, 5xx는 지수 백오프(3→6→12초)
  - tf_news, tf_analyst, tf_crypto 모두 `geminiWithRetry()`로 일원화

- **죽은 코드 제거**
  - `tools/collectors/supply_snapshot.js` (100줄) — supply-collect.yml 폐지 후 호출처 없음
  - `tools/publishers/slack.js` (51줄) — 호출처 없음
  - `tools/generators/` 폴더 통째 삭제 (`news_summarizer.js`, `report_generator.js`, 182줄) — main.js만 사용
  - `tools/main.js` — generators import 제거, 발송 경로는 친절한 안내 후 종료 메시지로 교체 (dry-run 경로는 유지)

### 검증 결과

- 로컬 orchestrator dry-run: KOSPI 7,584.6 (-396.81), KOSDAQ 1,142.4 (-48.69), 거래대금 41.39조 — 모두 Naver 폴백으로 정상 수집
- 모든 모듈 import 체인 정상 (`✅ 모든 모듈 로드 성공`)
- editor.js 단독 테스트: 휴장일 헤드라인 정상 생성

### 미완 (별도 세션)

- **designer.js 1457줄 분할** — 사주 결재로 별도 세션에서 진행

### 관련 오답노트

- `#033` GA 진입점 문서-실제 불일치 (2026-05-15 1차 작업)

---

### 사고 발견 — 어제 패치 60%가 GA에 반영 안 됨

사주가 받은 2026-05-15 메일 리포트에서 KOSPI/KOSDAQ N/A, 애널리스트 섹션 누락, "코스피 7981선" 헤드라인이 5거래일 추이 데이터(7,844.01)와 불일치 확인. 원인은 GA가 `orchestrator.js` 단독 실행으로 전환된 사실을 CLAUDE.md가 반영하지 않아 편집장이 `preview_send.js`·`main.js`에만 수정을 가한 것. 자세한 내용은 오답노트 `#033`.

### 정상화 작업 (완료)

- **orchestrator.js Layer 2 폴백 체인 이식** (`tools/orchestrator.js`)
  - 애널리스트 Gemini 503 시 3초 대기 후 1회 재시도 추가
  - 재시도 실패 시 원시 DART 리포트로 findings 자동 생성 (섹션 누락 방지)
  - DART URL 매칭 — findings.company 일치 시 dart_url 자동 채움
  - `runTFAnalyst(dart, news)` — 두 번째 인자 news 추가 호출

- **pipeline KOSPI 종가 폴백 추가** (`tools/pipeline/index.js`)
  - `_fetchNaverIdxLive(symbol)` 헬퍼: Naver m.stock 인덱스 라이브 API로 KOSPI/KOSDAQ today·prev·diff 추출
  - `_applyLiveFallbacks`에 KOSPI/KOSDAQ 종가 폴백 추가 (Yahoo ^KS11 실패 시 즉시 Naver로 보완)
  - kospiHistory 기반 추가 폴백 — Naver/Yahoo 모두 실패해도 마지막 거래일 종가로 N/A 방지
  - 로컬 dry-run 검증: 한국 휴장일에도 KOSPI 7733.92 (-247.49), KOSDAQ 1161.58 (-29.51) 정상 추출

- **헤드라인 데이터 검증** (`tools/desk/editor.js`)
  - `_validateHeadline(headline, kospi)` — 헤드라인의 KOSPI 인용값이 실제 데이터와 200p 또는 2% 이상 어긋나면 정정
  - 프롬프트에 명시적 헤드라인 작성 규칙 추가 (KOSPI 데이터 없을 때 KOSPI 수치 인용 금지)
  - 뉴스 본문 숫자를 무비판 인용하는 사고 차단

- **CLAUDE.md 진입점 명시** (`CLAUDE.md`)
  - "⚠️ 진입점 (Single Entry Point)" 섹션 신설 — `orchestrator.js`가 GA 단독 진입점임을 강조
  - 핵심 파일 참조 표 정정: `main.js`·`preview_send.js`에 `(deprecated)` 명시
  - 주요 명령어 섹션 정정: `node tools/orchestrator.js --now`를 1순위로
  - 개발 가이드라인: 신규 기능 추가 위치를 orchestrator 경로(pipeline/teams/desk)로 명시

- **에이전트 정의 정정** (`.claude/agents/orchestrator.md`, `desk.md`)
  - orchestrator: GA 단독 진입점임을 명시, main.js/preview_send.js 수정 금지 표기
  - desk: 책임 파일에서 `preview_send.js` 제거, 진입점 주의 블록 추가
  - description 필드에도 "Do NOT modify tools/preview_send.js or tools/main.js" 명시

- **레거시 파일 DEPRECATED 헤더** (`tools/main.js`, `tools/preview_send.js`)
  - 파일 상단에 deprecated 안내 주석 추가 + 오답노트 #033 참조

- **오답노트 #033 추가, 본 작업일지 항목 추가**

### 기대 효과

내일(2026-05-16) GA 실행 시:
- KOSPI/KOSDAQ N/A 사고 재발 방지 (Naver 폴백 자동 작동)
- 애널리스트 섹션 누락 사고 재발 방지 (Gemini 503 → 재시도 → DART 폴백)
- 헤드라인이 실제 시장 데이터와 일치하도록 검증

### 관련 오답노트

- `#033` GA 진입점 문서-실제 불일치 — 어제 패치 60%가 GA에 반영 안 됨

---

## 2026-05-14

### 완료

- **Gemini 유료 전환 시도** — 구글 본인인증 오류로 보류 (카드 명의자 이름 불일치 추정)

- **supply.json gitignore 수정** (`.gitignore`)
  - `outputs/` 한 줄이 `supply-collect.yml`의 `git add`를 묵묵히 실패시킴
  - `outputs/**` + `!outputs/` + `!outputs/*/` + `!outputs/*/supply.json` 패턴으로 변경
  - 오답노트 `#022` 추가

- **GA 이메일 TF 결과 단절 수정** (`publisher.js` + `orchestrator.js`)
  - `publisher.js`: `buildEmailCard(data, {}, {}, url)` → `buildEmailCard(data, tfResults, editorialPlan, url)`
  - `orchestrator.js`: `publish()` 호출에 `reportUrl`, `tfResults`, `editorialPlan` 전달 추가
  - 오답노트 `#023` 추가

- **preview_send.js 코인 섹션 고정 숨김 제거**
  - `include_crypto: false` → `include_crypto: !!(data.crypto)` — 데이터 있으면 자동 표시

- **git push origin main** — 커밋 `e99d11f..5de3444` push 완료

- **뉴스 선별 로직 통일** (`designer.js` → `_reorderNewsByTF` + `buildHtml`)
  - HTML 리포트 뉴스 테이블을 TF findings의 `source_url` 매칭으로 중요도순 정렬
  - TF 결과 없으면 기존 카테고리순 유지 (폴백 동작 유지)

- **KOSPI 시가총액 전일 비교값** (`domestic.js` + `designer.js`)
  - `domestic.js`: prevOutputDir의 `data.json`에서 전일 marketCap 로드 → `kospi.prevMarketCap`, `marketCapDiff`, `marketCapPct` 추가
  - `designer.js`: `d.marketCap`(버그 — 항상 undefined) → `d.kospi.marketCap` 수정, `trow()`로 전일값·변동 표시

- **코인 표 inline style 전환** (`designer.js` → `_buildCryptoSection`)
  - CSS 클래스(`class="c"`, `class="r"`, `class="chg-val"`) 제거 → 전체 inline style
  - `display:flex` + `var(--color-text-secondary)` → `<table>` 기반 footer row로 교체

- **git push origin main** 완료 (`60e5645..deb0bba`)

- **이메일 카드뉴스 개선** (`tools/teams/tf_news.js` + `tools/desk/designer.js`)
  - `tf_news.js`: findings에 `summary` 필드(2-3 bullet) 추가 — Gemini가 기사당 핵심 포인트 생성
  - `designer.js` `buildEmailCard` 뉴스 섹션:
    - 원문 제목 매칭: `f.source_url` → rawNews URL 조회 → 원문 제목 사용 (TF 재작성 제목 대체)
    - bullet 요약 표시: `f.summary` 배열을 `• 포인트` 형식으로 렌더링
  - `designer.js` `buildEmailCard` 애널리스트 섹션 추가:
    - 상위 3개 findings: 회사명·증권사·투자의견·목표가 변동·핵심 논거 표시
    - 섹션 순서: 시장 요약 → 지수 → 환율/원자재 → 코인 → 뉴스 → **애널리스트**

- **main.js crypto/DART 수집 추가** (`tools/main.js`)
  - `collectCrypto`, `collectDart` import 및 `Promise.all`에 병렬 추가
  - `validateData()` 후 `data.crypto`, `data.dart` 수동 머지 → `data.json`에 포함
  - 이전: 로컬 `data.json`에 crypto/dart = null → 이메일 카드에 코인·애널리스트 미표시 (해결)

- **preview_send.js TF-Crypto 추가** (`tools/preview_send.js`)
  - `runTFCrypto` import 및 TF-Analyst와 병렬 실행 추가
  - `buildHtml`, `buildEmailCard` 호출에 `crypto: data.crypto`, `crypto: tfCryptoResult` 전달

- **designer.js 5항목 개선** (`tools/desk/designer.js`)
  - 항목 1: `buildEmailCard` 뉴스 카드에 원문 URL 링크 추가 — url 있으면 `<a>` 태그, 없으면 기존 `<div>` 유지. rawNews fallback에도 `url` 필드 추가
  - 항목 2: KOSPI 5거래일 테이블에 `<colgroup>` 추가, 주요 이슈 열 `min-width:120px` 적용 (날짜14%·종가11%·전일比11%·등락률9%·거래대금12%·이슈43%)
  - 항목 3: `_buildHistIssues` 프롬프트를 "모든 날짜에 15~30자 기재" 로 변경, `issueStr` 빈문자열도 폴백 처리(`??` → `||`)
  - 항목 4: `_buildRowNotes` 비고 50자 이내로 확대, 수치+원인 키워드 병기 지시 추가, top_stories 3개로 확대
  - 항목 5: `_buildCryptoSection` 테이블에 "시장 동향" 열 추가(BTC행에 market_summary 앞 25자), market_summary 표시 블록 스타일을 `#f0f4ff` 배경·파란 border·13px로 개선

- **VKOSPI investing.com 스트리밍 파싱** (`domestic.js`)
  - Naver 409 영구 차단 확인 → `kr.investing.com/indices/kospi-volatility` 189KB 스트리밍
  - `data-test="instrument-price-last"`, `data-test="instrument-price-change"` 파싱
  - US VIX 폴백 완전 제거 (사주 지시: 대리 지표 제공 금지)
  - 오답노트 `#028` 추가

- **supply-collect.yml 중복 제거**
  - `preview_send.js`의 `fetchSupplyHistory()`가 이미 직접 수집 → 16:40 GA 크론 불필요
  - `supply-collect.yml` 삭제, `main.js` STEP 2.5 제거, `.gitignore` supply.json 예외 제거
  - 오답노트 `#029` 추가

- **KOSPI prevMarketCap 항상 null 수정** (`domestic.js`, `designer.js`)
  - `prevOutputDir/data.json`에서 전일 marketCap 로드 (파일 없으면 null)
  - `d.kospi.marketCap` 오타 → `d.marketCap` 수정

- **비고 열 모두 빈값 → 데이터 기반 폴백 추가** (`designer.js`)
  - `_buildRowNotesFallback()` 신규 함수: 방향·변동률·환율 레벨·VKOSPI 임계값·수급 요약을 수치에서 생성
  - `_buildRowNotes()`: 폴백 먼저 생성 후 Gemini로 보완, Gemini 503 시 폴백 반환
  - `_buildHistIssues()`: 등락률 계산 결과로 데이터 폴백 생성 (소폭=보합, ±1.5% 이상=대폭)
  - 오답노트 `#025` 추가

- **코인 표 3가지 버그 수정** (`designer.js` → `_buildCryptoSection`)
  - BTC·ETH 순위 열: 하드코딩 `—` → `top10.find()`.rank 실제 값 조회
  - 시장동향 열: `tfCrypto.findings`에서 `asset` 매칭으로 코인별 `key_level` 추출
  - 컬럼 너비: `6%/14%/24%/18%/38%` → `5%/10%/22%/15%/48%`
  - 심볼 열: `coin.symbol ?? coin.name` 폴백 추가
  - 오답노트 `#026` 추가

- **애널리스트 섹션 Gemini 503 시 완전 누락 수정** (`preview_send.js`)
  - `include_analyst` 조건을 `findings.length > 0` → `dart.reports.length > 0`으로 변경
  - DART 폴백: Gemini 실패 시 `dart.reports`를 `findings` 형식으로 변환해 섹션 유지
  - 오답노트 `#027` 추가

- **수급추이 표 간격·제목 개선** (`designer.js` → `_buildSupplyHistory`)
  - `margin-top:4px` → `margin-top:20px; padding-top:16px; border-top:1px solid #e8eaed`
  - 제목 스타일 `.st`(소형) → `font-size:13px; font-weight:700` 강조
  - 오답노트 `#031` 추가

- **`include_analyst` 조건 오류 재수정** (`preview_send.js`)
  - `data.dart?.reports?.length > 0` 오조건 → `tfAnalystResult.findings?.length > 0` 복원
  - 오답노트 `#030` 추가

- **tf_analyst Gemini 503 재시도 로직 추가** (`preview_send.js`)
  - findings 빈 경우 + 뉴스 소스 있으면 3초 대기 후 1회 재호출
  - 재시도 실행 후 3건 분석 성공 확인
  - 오답노트 `#032` 추가

- **애널리스트 리포트 링크 처리** (`designer.js`)
  - HTML 리포트·이메일 카드 모두: `dart_url` 있으면 회사명 → DART 링크
  - DART 폴백 찾기: `preview_send.js`에서 findings 완료 후 `company` 필드로 dart.reports 매칭

### 미완 / 다음 세션 이어서

- Gemini 유료 전환 재시도 (구글 본인인증 해결 후)

### 관련 오답노트

- `#022` supply.json gitignore 누락
- `#023` orchestrator.js→publisher.js TF 결과 단절
- `#024` 이메일 카드 TF 재작성 제목·요약 미흡 (원문 매칭 + bullet 요약으로 해결)
- `#025` Gemini 503 → 비고 열 빈값 (데이터 기반 폴백으로 해결)
- `#026` 코인 표 3가지 오류 (수정 완료)
- `#027` 애널리스트 Gemini 503 → 섹션 누락 (DART 폴백으로 해결)
- `#028` VKOSPI Naver 409 → investing.com 전환
- `#029` supply-collect.yml 중복 제거
- `#030` include_analyst 조건 오류 재발
- `#031` 수급추이 간격·스타일 미흡
- `#032` 애널리스트 Gemini 503 재시도 없음 → 재시도 추가

---

## 2026-05-13

### 완료

- **이메일 카드 Morning Brew 스타일 완성** (`tools/desk/designer.js` → `buildEmailCard`)
  - 지수 카드 방향별 틴팅 배경 (UP: `#fff8f8` / DOWN: `#f0f5ff` / FLAT: `#f8f9fb`)
  - 상단 시장 요약 섹션 추가 (Gemini `top_stories` 기반, 데이터 없으면 숨김)
  - 뉴스 기사별 `market_impact` 한 줄 요약 표시
  - 이메일 한국 증시 색상 관례 적용: 상승=빨강(`#E24B4A`), 하락=파랑(`#378ADD`)

- **GitHub Pages 404 영구 수정**
  - 원인: gh-pages `.gitignore`에 `outputs/` 포함 → 배포 파일이 커밋에서 제외됨
  - gh-pages `.gitignore`에서 `outputs/` 라인 제거
  - 2026-05-08 ~ 2026-05-13 report.html 수동 배포

- **HTML 리포트 반응형 전환** (`tools/desk/designer.js` → `_assembleHtml` CSS)
  - `max-width` 720px → 1100px (PC 와이드 화면)
  - 모든 테이블(8개)에 `.tbl-wrap{overflow-x:auto}` 래퍼 추가
  - `@media(max-width:900px)`: 패딩 축소, `.td-sum` 숨김
  - `@media(max-width:600px)`: 폰트·패딩 축소, 수급 그리드 1열, 테이블 가로 스크롤
  - main 커밋 `9cc5800` + gh-pages 배포 완료

- **이메일 카드 뉴스 제목 truncate + 카드 높이 균일화** (`buildEmailCard` newsRows)
  - JS `truncateTitle(str, 50)` 헬퍼로 50자 초과 제목 말줄임표 처리 (Gmail CSS line-clamp 미지원 대응)
  - 각 카드에 `min-height:72px; box-sizing:border-box` 인라인 스타일 추가
  - `market_impact` 없을 때 18px 빈 placeholder 삽입 → 있을 때/없을 때 높이 편차 최소화
  - `outputs/email_preview.html` 재생성 완료

- **이메일 카드 flex → table 전환 (Gmail 호환)** (`tools/desk/designer.js` → `buildEmailCard`)
  - Gmail이 `display:flex`를 불안정하게 처리해 카드 너비가 제각각이던 근본 원인 해결
  - 헤더·섹션구분선·증시(2열)·환율원자재(3열)·코인(3열)·요약 bullet 전부 `<table>` 교체
  - 데이터 누락 시 빈 `<td>`가 자리 유지 → 행간 너비 불일치 방지
  - `display:flex` 완전 제거 (0개 잔존), Gmail 실발송 균일 확인 완료
  - 커밋 `6e75611`

- **이전 미완 항목 처리 완료**
  - 이메일 카드 실제 발송 테스트 (sent.flag 삭제 후 재발송 성공)
  - Gemini 429 쿼터 소진 시 요약 섹션 비표시 동작 확인 (정상)

- **git push origin main 실행** — 누적된 14개 커밋 GitHub 반영 완료 (`25b08e8..6e75611`)
  - 원인: 코드 수정 후 push를 반복적으로 누락 → GA가 구버전으로 실행
  - CLAUDE.md 세션 종료 체크리스트에 push 필수 항목 추가
  - 오답노트 `#021` 추가

### 미완 / 다음 세션 이어서

- 없음 (모든 커밋 push 완료)

### 관련 오답노트

- `#013` 내용 미확정 상태에서 구현 선진행
- `#014` 깨진 구조 위에 반복 패치
- `#015` preview 미갱신
- `#016` 에이전트 과다 위임
- `#017` GitHub Pages 404
- `#018` gh-pages 브랜치 전환 충돌
- `#019` gh-pages에서 main 소스 실행 불가
- `#020` Gmail flex 레이아웃 붕괴 → table 전환

---
